{
  "name": "wwdc-reports",
  "version": "0.1.0",
  "description": "No-key WWDC raw metadata and transcript archiver.",
  "type": "module",
  "bin": {
    "wwdc-reports": "./bin/wwdc-reports.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/wwdc-reports.js && find src test -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "wwdc",
    "apple",
    "archive",
    "metadata",
    "no-key"
  ],
  "license": "MIT"
}
