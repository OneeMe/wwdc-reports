---
title: "Add rich graphics to your SwiftUI app"
description: "learn how you can bring your graphics to life with swiftui. we"
date: 2026-06-10
tags: ["WWDC 2021"]
thumbnail: "/images/sessions/2021/10021.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2021/10021/"
sessionId: "wwdc2021-10021"
year: "2021"
relatedSessions:
  - title: "Bring Recurring Leaderboards to your game"
    code: "10067"
    description: "recurring leaderboards are a great way to encourage players to keep coming back "
  - title: "Capture and process ProRAW images"
    code: "10160"
    description: "when you support proraw in your app, you can help photographers easily capture a"
  - title: "Create 3D models with Object Capture"
    code: "10076"
    description: "object capture provides a quick and easy way to create lifelike 3d models of rea"
  - title: "Create 3D workflows with USD"
    code: "10077"
    description: "discover the flexibility, versatility and power of pixar"
---

# Add rich graphics to your SwiftUI app

---

## Highlight

> SwiftUI 的图形能力在 iOS 15 得到了显著增强。Session 介绍了两个全新的组件：`Canvas` 用于高性能的自定义绘制，`TimelineView` 用于按时间表更新视图。这两个组件的组合使得在 SwiftUI 中创建复杂的数据可视化和动态图形成为可能。

---

## 核心内容

SwiftUI 的图形能力在 iOS 15 得到了显著增强。Session 介绍了两个全新的组件：`Canvas` 用于高性能的自定义绘制，`TimelineView` 用于按时间表更新视图。这两个组件的组合使得在 SwiftUI 中创建复杂的数据可视化和动态图形成为可能。

`Canvas` 是一个 immediate-mode 的绘制 API，类似于 UIKit 中的 `drawRect`，但它天然支持 SwiftUI 的动画系统。你可以在 Canvas 中使用 `GraphicsContext` 来绘制路径、图片、文字，所有操作都发生在 GPU 上。

`TimelineView` 则是一个按时间表刷新的容器视图。传统的 SwiftUI 动画只能做预设路径的插值，而 `TimelineView` 可以让你根据时间精确控制每一帧的绘制内容。配合 `Canvas`，这就等于在 SwiftUI 中拥有了一个轻量级的渲染引擎。


### 最佳实践

`Canvas` 的绘制闭包中不要创建复杂对象。每次重绘时闭包都会被调用，如果你在里面创建大量的 `Path` 或者 `CGAffineTransform`，GC 压力会很大。建议把不变的计算结果缓存到视图的属性中，闭包只做简单的绘制调用。

`TimelineView` 配合 `.animation()` 修饰符时要注意：TimelineView 本身的刷新不是动画驱动的，它是基于时间表的硬刷新。如果你想要平滑的过渡效果，需要在 Canvas 内部用插值来实现，而不是依赖 SwiftUI 的 `.animation()`。

---

## 相关 Session

- [Bring Recurring Leaderboards to your game](/articles?year=2021&topic=all&search=10067) — recurring leaderboards are a great way to encourage players to keep coming back 
- [Capture and process ProRAW images](/articles?year=2021&topic=all&search=10160) — when you support proraw in your app, you can help photographers easily capture a
- [Create 3D models with Object Capture](/articles?year=2021&topic=all&search=10076) — object capture provides a quick and easy way to create lifelike 3d models of rea
- [Create 3D workflows with USD](/articles?year=2021&topic=all&search=10077) — discover the flexibility, versatility and power of pixar
