---
title: "Detect Body and Hand Pose with Vision"
description: "explore how the vision framework can help your app detect body and hand poses in photos and video. with pose detection, your app can analyze the poses, movements, and gestures of people to offer new v"
date: 2026-06-10
tags: ["WWDC 2020"]
thumbnail: "/images/sessions/2020/10653.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2020/10653/"
sessionId: "wwdc2020-10653"
year: "2020"
relatedSessions:
  - title: "Explore the Action & Vision app"
    code: "10099"
    description: "it"
  - title: "Get models on device using Core ML Converters"
    code: "10153"
    description: "with core ml you can bring incredible machine learning models to your app and ru"
  - title: "Make apps smarter with Natural Language"
    code: "10657"
    description: "explore how you can leverage the natural language framework to better analyze an"
---

# Detect Body and Hand Pose with Vision

---

## Highlight

> iOS 14 的 Vision 框架新增了三个关键的人体分析能力：Body Pose Estimation（身体姿态估计）、Hand Pose Estimation（手部关节检测）以及基于姿态的动作分类。

---

## 核心内容

iOS 14 的 Vision 框架新增了三个关键的人体分析能力：Body Pose Estimation（身体姿态估计）、Hand Pose Estimation（手部关节检测）以及基于姿态的动作分类。

Body Pose 可以从图像或视频帧中检测 17 个人体关键点（头、肩、肘、腕、髋、膝、踝等），每个点包含位置坐标和置信度。Hand Pose 更精细，检测单只手的 21 个关节点，包括每根手指的各个关节弯曲角度。这些检测全部在 Neural Engine 上运行，帧率可达 30fps 以上。

Session 演示了如何用 `VNDetectHumanBodyPoseRequest` 和 `VNDetectHumanHandPoseRequest` 获取关键点数据，以及如何基于连续帧的关键点序列做动作识别（比如识别「挥手」「点赞」等手势）。


### 最佳实践

1. **实时场景用 AVCaptureSession 配合 Vision**。每帧用 `VNImageRequestHandler` 处理 `CVPixelBuffer`，在 `captureOutput` 回调中执行。注意不要在主线程跑 Vision 请求，用单独的 dispatch queue。
2. **关键点要做平滑处理**。原始关键点会有帧间抖动，用低通滤波（exponential smoothing）或卡尔曼滤波器平滑位置数据，否则基于关键点的 UI 会不停抖动。
3. **身体和手部检测不要同时开**。两个请求都跑在 Neural Engine 上，同时执行会争抢资源，帧率减半。根据场景切换——全身场景用 Body Pose，近景交互用 Hand Pose。

---

## 相关 Session

- [Explore the Action & Vision app](/articles?year=2020&topic=all&search=10099) — it
- [Get models on device using Core ML Converters](/articles?year=2020&topic=all&search=10153) — with core ml you can bring incredible machine learning models to your app and ru
- [Make apps smarter with Natural Language](/articles?year=2020&topic=all&search=10657) — explore how you can leverage the natural language framework to better analyze an
