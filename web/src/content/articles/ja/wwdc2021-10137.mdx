---
title: "改进 App 和图书的 MDM 分配"
description: "リアルタイム通知、非同期処理、大規模なアプリ配布を効率的に管理する方法など、Apps and Books Management API の最新の改善点について学びます。"
date: "2021-06-08"
tags: ["App Store", "Distribution & Marketing", "Business & Education"]
thumbnail: "/images/sessions/2021/10137.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2021/10137/"
sessionId: "wwdc2021-10137"
year: "2021"
relatedSessions:
  - title: "管理 Apple 设备的新特性"
    code: "10130"
    description: "管理対象アプリと必須アプリのインストール メカニズム"
  - title: "探索 account-driven User Enrollment"
    code: "10136"
    description: "BYOD シナリオのアプリケーション割り当て"
  - title: "在组织中管理软件更新"
    code: "10129"
    description: "アプリケーションとシステムのアップデートの共同管理"
  - title: "使用 Apple Configurator 管理设备"
    code: "10297"
    description: "デバイスの構成とアプリケーションの導入"
---
# アプリと書籍の MDM 配布を改善する

## ハイライト

> 新しい Apps and Books Management API では、リアルタイム通知と非同期処理が導入され、250,000 のアプリ割り当てが最低 25,000 リクエストからわずか 10 リクエストに削減されます。 1 回のリクエストで最大 25 個のアプリを 1,000 台のデバイスに割り当てることができ、割り当てステータスはプッシュ通知を通じてリアルタイムで配信されます。

## 主要内容

企業や教育機関へのアプリケーション配布の管理は、常に拡張の課題となっています。生徒数 10,000 人の学校では、生徒 1 人につき 1 台のデバイスを使用すると、25 個のコア アプリケーションを配布する必要があり、合計 250,000 個の配布が必要になります。従来の API は、リクエストごとに最大 10 台のデバイスの単一アプリ割り当てを処理しました。これは、最低 25,000 の HTTP リクエストを意味します。

WWDC2021 で導入されたアプリと書籍管理 API の新バージョンは、2 つの主要な改善によりこの問題を解決します。それは、リアルタイム通知により継続的なポーリングの必要性がなくなり、非同期処理によりサーバー側で割り当て操作を並行して実行できるようになります。同じ 250,000 の割り当てに必要なリクエストはわずか 10 件になりました。

＃＃詳細

### リアルタイム通知タイプ

API の新しいバージョンは 4 つの通知タイプをサポートしており、クライアント設定の sToken によって個別にサブスクライブする必要があります (01:20)。

#### 資産管理に関する通知

アソシエート (割り当て)、関連付け解除 (割り当て解除)、取り消し (取り消し) イベント (01:44) など、割り当てステータスが変更されたときにトリガーされます。

```json
{
  "notification": {
    "assignments": [
      {
        "adamId": "408709785",
        "pricingParam": "STDQ",
        "serialNumber": "C02Y402HLCM6"
      }
    ],
    "eventId": "87cbc650-16cc-4f9e-a833-e622f377a9f7",
    "result": "SUCCESS",
    "type": "ASSOCIATE"
  },
  "notificationType": "ASSET_MANAGEMENT",
  "uId": "2049025000431439"
}
```

通知を受信したら、すべてのバッチ操作が完了するのを待たずに、対応するデバイスにインストール コマンドを送信できます。

#### アセット数の通知

購入、転送、または払い戻しの結果としてアプリの数量が変更されるとトリガーされます (03:37):

```json
{
  "notification": {
    "adamId": "408709785",
    "countDelta": 50,
    "pricingParam": "STDQ"
  },
  "notificationType": "ASSET_COUNT",
  "uId": "2049025000431439"
}
```

`countDelta`正の値は購入または送金を示し、負の値は払い戻しまたは送金を示します。 MDM は、同期エンドポイントを呼び出さずに、この増分でローカル数を直接更新できます。

#### ユーザー管理通知

作成、更新、および引退イベントを含む、登録済みユーザーのステータスが変更されるとトリガーされます (05:34):

```json
{
  "notification": {
    "users": [
      {
        "clientUserId": "client-100",
        "idHash": "leSKk3IaE2vk2KLmv2k3/200D3=",
        "status": "Associated"
      }
    ],
    "eventId": "e0def1f8-9158-4343-9c52-8dd32da50b9b",
    "result": "SUCCESS",
    "type": "UPDATE"
  },
  "notificationType": "USER_MANAGEMENT"
}
```

#### ユーザー関連の通知

ユーザーが招待を受け入れ、個人の Apple ID を登録済みアカウントに関連付けるときにトリガーされます (08:33):

```json
{
  "notification": {
    "associatedUsers": [
      {
        "clientUserId": "client-100",
        "idHash": "leSKk3IaE2vk2KLmv2k3/200D3=",
        "inviteCode": "f551b37da07146628e8dcbe0111f0364",
        "status": "Associated"
      }
    ]
  },
  "notificationType": "USER_ASSOCIATED"
}
```

### 通知サブスクリプションの構成

クライアント設定で通知受信を設定します (09:00):

- `notificationAuthToken`: 通知の Authorization ヘッダーで Apple によって使用される共有シークレット
- `notificationUrl`: 通知を受信する HTTPS エンドポイント

通知の受信後に HTTP 200 が返された場合は、配信が成功したことを示します。 200 以外の応答またはタイムアウトの場合、Apple は数回再試行します (ベストエフォート)。通知を長期間受信できない場合は、期限切れの可能性があるステータスについてポーリングと同期を実行する必要があります。

### 非同期処理

従来の API のすべての操作は同期的でした。リクエストを送信し、Apple が割り当てを完了するのを待ってから応答を返しました (10:07)。新しいバージョンは非同期処理に変更されています。

```
旧版流程：
请求 → Apple 同步执行分配 → 返回结果（单次最多 10 台设备 × 1 个应用）

新版流程：
请求 → Apple 立即返回 eventId → 后台异步处理 → 通过通知推送结果
（单次最多 1,000 台设备 × 25 个应用）
```

非同期処理の利点 (10:25):
- 処理速度を最適化するためにサーバー側で強制並列処理
- 秩序ある処理により、断続的な失敗と再試行が減少します。
- 大規模な導入の信頼性が向上し、教育機関が新学期中に導入しやすくなります。

### 実際の比較: 250,000 割り当て

10,000 ユーザー × 25 アプリケーションのシナリオを例に挙げます (11:08):

|メトリクス |古い API |新しい API |
|------|----------|----------|
|単一リクエストの処理能力 | 1アプリケーション×10台 | 25 アプリケーション × 1,000 デバイス |
|リクエストの最小数 | 25,000 | 10 |
|応答モード |同期待ち |すぐにeventIdを返す |
|結果取得 |レスポンスボディから直接取得 |通知プッシュまたはクエリ イベント ステータス エンドポイント |

### リクエスト形式の変更

新しい API は、バージョン管理された URI と正しい HTTP メソッドを使用します (04:34)。すべてのリクエストは、Authorization ヘッダーにベアラー トークン形式で sToken を運びます。

```
Authorization: Bearer <sToken>
```

アセットのステータスを取得します (GET リクエスト):

```
GET /v1/assets?adamId=408709785&pricingParam=STDQ
```

応答には、ページネーション情報とアセット数が含まれます。

```json
{
  "assets": [
    {
      "adamId": "408709785",
      "pricingParam": "STDQ",
      "assignedCount": 5000,
      "availableCount": 10000,
      "totalCount": 15000
    }
  ],
  "currentPageIndex": 0,
  "size": 1,
  "totalPages": 1
}
```

アセットの割り当て (POST リクエスト):

```json
// 请求
{
  "assets": [
    { "adamId": "361309726", "pricingParam": "STDQ" }
  ],
  "serialNumbers": [
    "serial-1", "serial-2", ..., "serial-1000"
  ]
}

// 响应
{
  "eventId": "92467a8e-8a50-4df9-9b30-f7ff4a99dea7",
  "tokenExpirationDate": "2021-07-06T14:12:10+0000"
}
```

### 秩序ある処理

新しい API により、同じイベント内の操作が送信された順序 (13:49) で実行されることが保証されます。たとえば、再配布: 最初に関連付け解除リクエストを送信してeventId1を取得し、次に関連付けリクエストを送信してeventId2を取得します。 Apple は、eventId2 の操作を開始する前に、eventId1 のすべての操作が完了していることを確認します。

### Sync API のバージョン識別

すべての同期 API 応答には次のものが含まれます`versionId`(07:28)、基礎となるデータが書き込まれると変更されます。多数のページ分割された応答を走査する場合、versionId を使用して、前のページのデータが変更されたかどうかを検出できます。

## 重要ポイント

1. **新しいバージョンの API にすぐに移行します**: 古いバージョンの API は引き続きサポートされますが、同じ sToken の古いバージョンと新しいバージョンを混在させることは正式にサポートされていません。新しい API の非同期処理とバッチ機能が前進する唯一の方法です。

2. **展開通知受信エンドポイント**: 構成`notificationUrl`そして`notificationAuthToken`、4 つの通知タイプすべてを購読します。これは、ポーリングを排除し、リアルタイムの状態同期を可能にする鍵となります。

3. **バッチ リクエストを使用して呼び出し数を削減します**: 1 回のリクエストで最大 25 アプリケーション × 1,000 デバイス。リクエストの数を最小限に抑えるためにバッチを合理的に編成および配布します。

4. **eventId を使用して非同期操作を追跡します**: 割り当てリクエストの送信後に返されたeventIdを記録し、通知またはイベント ステータス エンドポイントを通じて進行状況を追跡します。成功通知を受信したら、インストール コマンドをデバイスに送信します。

5. **通知配信失敗の処理**: Apple の通知はベストエフォート配信です。サービスが利用できず、通知が失われた場合は、完全同期ではなく、期限切れの可能性があるステータスを対象としたポーリングを実行します。

## 関連セッション

- [WWDC2021 10130 - Apple デバイスを管理するための新機能](https://developer.apple.com/videos/play/wwdc2021/10130/) — 管理対象アプリと必要なアプリのインストール メカニズム
- [WWDC2021 10136 - アカウント主導のユーザー登録の探索](https://developer.apple.com/videos/play/wwdc2021/10136/) — BYOD シナリオのアプリケーション割り当て
- [WWDC2021 10129 - 組織内のソフトウェア アップデートの管理](https://developer.apple.com/videos/play/wwdc2021/10129/) — アプリケーションとシステムのアップデートの共同管理
- [WWDC2021 10297 - Apple Configurator を使用してデバイスを管理する](https://developer.apple.com/videos/play/wwdc2021/10297/) — デバイスの構成とアプリケーションの展開
