---
title: "Unwrap PaperKit"
description: "craft a canvas-based application with paperkit. explore the new data model apis that let you access, create, and modify markup elements. learn how to add custom controls and annotations, and discover "
date: 2026-06-10
tags: ["PaperKit", "PencilKit", "Canvas", "Adornments", "UIKit"]
thumbnail: "/images/sessions/2026/372.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/372/"
sessionId: "wwdc2026-372"
year: "2026"
relatedSessions:
  - title: "Deliver workout insights with HealthKit workout zones"
    code: "207"
    description: "healthkit makes it easier to provide workout insights — like heart rate and cycl"
  - title: "Enhance the accessibility of your reading app"
    code: "219"
    description: "learn how to create robust reading experiences for voiceover, speak screen, and "
  - title: "Live Activities essentials"
    code: "223"
    description: "elevate your app experience with live activities. explore many of the places whe"
  - title: "Modernize your AppKit app"
    code: "289"
    description: "bring your appkit app up to date with modern macos conventions. dive into handli"
---

# Unwrap PaperKit

---

## Highlight

> 以前想做个支持手写、贴图、还能无限缩放的画布，要么自己用 `UIScrollView` 加 `CALayer` 拼凑到手秃，要么用第三方库忍受各种性能玄学。现在 Apple 直接把内部用了多年的 PaperKit 开放了。它把画布内容抽象成了 `PaperMarkup`，里面的元素（形状、图片、笔迹）都放在一个有序的集合（`MarkupOrderedSet`）里，你可以像操作数组一样去增删改查。

---

## 核心内容

以前想做个支持手写、贴图、还能无限缩放的画布，要么自己用 `UIScrollView` 加 `CALayer` 拼凑到手秃，要么用第三方库忍受各种性能玄学。现在 Apple 直接把内部用了多年的 PaperKit 开放了。它把画布内容抽象成了 `PaperMarkup`，里面的元素（形状、图片、笔迹）都放在一个有序的集合（`MarkupOrderedSet`）里，你可以像操作数组一样去增删改查。

最绝的是它引入了 Adornments（装饰物）概念。以前在画布上放个“删除”按钮或者“添加图片”的悬浮控件，你得自己算坐标、处理缩放比例、还得在导出 PDF 时把这些 UI 控件剔除掉。现在直接丢进 Adornments 里，它自动跟随画布坐标系，且绝对不会被序列化进文档里。这对做批注、协作白板类 App 的团队来说，直接省了至少一个月的打磨时间。


### 最佳实践

已有项目如果你的 App 已经用 PencilKit 做了手写，但外面套了一层自己写的 ScrollView 和图层管理，建议逐步把外层容器替换为 `PaperMarkupViewController`。笔迹部分无缝衔接，但形状和图片的管理可以大幅简化，直接把现有的自定义 Model 映射到 `ShapeMarkup` 或 `ImageMarkup` 即可。

新项目无脑用 PaperKit。但要注意，PaperKit 强依赖 UIKit，如果你在纯 SwiftUI 项目里用，需要用 `UIViewControllerRepresentable` 包一层。状态同步是个难点，建议把 `PaperMarkup` 作为单一数据源（Single Source of Truth），在 SwiftUI 的 `updateUIViewController` 里做 diff 更新，而不是双向绑定。

实战中容易踩的坑是 Z-axis 顺序管理。`MarkupOrderedSet` 是有序集合，图层的上下关系完全由数组索引决定。如果你想把某个元素置顶，不要去找 `zIndex` 属性，直接在 `subelements` 数组里把它 `remove` 然后 `append` 到末尾就行。

---

## 相关 Session

- [Deliver workout insights with HealthKit workout zones](/articles?year=2026&topic=all&search=207) — healthkit makes it easier to provide workout insights — like heart rate and cycl
- [Enhance the accessibility of your reading app](/articles?year=2026&topic=all&search=219) — learn how to create robust reading experiences for voiceover, speak screen, and 
- [Live Activities essentials](/articles?year=2026&topic=all&search=223) — elevate your app experience with live activities. explore many of the places whe
- [Modernize your AppKit app](/articles?year=2026&topic=all&search=289) — bring your appkit app up to date with modern macos conventions. dive into handli
