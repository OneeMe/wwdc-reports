---
title: "Create live communication experiences"
description: "livecommunicationkit transforms your real-time communication apps into integrated experiences. we'll show you how to deliver a rich, native conversation ui that puts your app right where people need i"
date: 2026-06-10
tags: ["LiveCommunicationKit", "PushKit", "CallKit", "VoIP", "Dynamic Island"]
thumbnail: "/images/sessions/2026/226.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/226/"
sessionId: "wwdc2026-226"
year: "2026"
relatedSessions:
  - title: "Build with the new Apple Foundation Model on Private Cloud Compute"
    code: "319"
    description: "private cloud compute lets you access powerful, frontier-class models while prot"
  - title: "Expand the capabilities of your Virtualization app"
    code: "224"
    description: "bring powerful new capabilities in macos 27 to your virtualization app. discover"
  - title: "Find your accessory with Bluetooth Channel Sounding"
    code: "369"
    description: "get started with channel sounding to bring distance and direction awareness to y"
  - title: "Rev up your CarPlay app"
    code: "212"
    description: "explore new features for your carplay audio, navigation, voice-based conversatio"
---

# Create live communication experiences

---

## Highlight

> 做过音视频通话的开发者都知道，旧的 `CXProvider` (CallKit 的核心类) 就像个黑盒，系统 UI 和 App 内部状态经常对不上，而且对群组通话和灵动岛的支持一直像在打补丁。Apple 这次直接掀桌子，推出了 `LiveCommunicationKit`。

---

## 核心内容

做过音视频通话的开发者都知道，旧的 `CXProvider` (CallKit 的核心类) 就像个黑盒，系统 UI 和 App 内部状态经常对不上，而且对群组通话和灵动岛的支持一直像在打补丁。Apple 这次直接掀桌子，推出了 `LiveCommunicationKit`。

新框架把核心概念从“电话 (Call)”变成了“对话 (Conversation)”。你只需要维护一个 `ConversationManager` 单例，通过 `ConversationAction` 来统一接管所有交互。不管是用户在锁屏全屏 UI 点了接听，还是在 App 里点了挂断，甚至是在灵动岛上滑动结束，都会变成同一个 Action 丢给你的 Delegate。这对所有做 1v1 语音、多人语音房、甚至游戏内实时沟通的 App 来说，是一次底层架构级的减负。


### 最佳实践

**已有项目的迁移策略**
不要试图在旧 `CXProvider` 和 `LiveCommunicationKit` 之间做无缝热切换。建议通过 Feature Flag 控制，在灰度版本中让两套代码并存。迁移的重头戏不是改 API 名字，而是重构你的状态机。把原本散落在 `CXProviderDelegate` 各个回调里的逻辑，全部抽离成纯粹的函数，然后在新的 `ConversationManagerDelegate` 的 `switch action` 中统一调用。

**新项目的采用建议**
直接上 `LiveCommunicationKit`。在架构设计时，把 `ConversationAction` 当作你信令层（Signaling Layer）的入口。App 内的 UI 按钮不要直接调用 WebRTC 的 API，而是构造一个 `StartConversationAction` 或自定义 Action 丢给 Manager，让 Manager 驱动底层逻辑。这样你的 App 内 UI 和系统 UI 就天然保持了行为一致。

**实战中容易踩的坑**
后台模式配置。必须在 Xcode 的 Background Modes 中同时勾选 `Audio, AirPlay, and Picture in Picture` 和 `Voice over IP`。漏掉任何一个，App 切到后台后音频流就会在几秒钟内被系统掐断。另外，`Handle` 的 `value` 尽量用真实的手机号或邮箱，这样系统才能正确匹配通讯录，在锁屏显示联系人头像，而不是显示一个干瘪的 UUID。

---

## 相关 Session

- [Build with the new Apple Foundation Model on Private Cloud Compute](/articles?year=2026&topic=all&search=319) — private cloud compute lets you access powerful, frontier-class models while prot
- [Expand the capabilities of your Virtualization app](/articles?year=2026&topic=all&search=224) — bring powerful new capabilities in macos 27 to your virtualization app. discover
- [Find your accessory with Bluetooth Channel Sounding](/articles?year=2026&topic=all&search=369) — get started with channel sounding to bring distance and direction awareness to y
- [Rev up your CarPlay app](/articles?year=2026&topic=all&search=212) — explore new features for your carplay audio, navigation, voice-based conversatio
