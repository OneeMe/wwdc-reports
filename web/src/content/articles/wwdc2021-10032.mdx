---
title: "Explore WKWebView additions"
description: "explore the latest updates to wkwebview. we"
date: 2026-06-10
tags: ["WWDC 2021"]
thumbnail: "/images/sessions/2021/10032.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2021/10032/"
sessionId: "wwdc2021-10032"
year: "2021"
relatedSessions:
  - title: "Accelerate networking with HTTP/3 and QUIC"
    code: "10094"
    description: "the web is changing, and the next major version of http is here. learn how http/"
  - title: "Adopt Quick Note"
    code: "10264"
    description: "learn how you can link your app to quick note and help people quickly connect yo"
  - title: "Develop advanced web content"
    code: "10030"
    description: "develop in javascript, webgl, or webassembly? learn how the latest updates to sa"
  - title: "Meet privacy-preserving ad attribution"
    code: "10033"
    description: "discover how you can measure your ad campaigns in apps and on the web without co"
---

## Highlight

> WKWebView 是 iOS app 中嵌入 Web 内容的标准组件。iOS 15 对它做了多项增强，覆盖了查找交互、JavaScript 互操作、内容安全和媒体能力四个方面。

---

## 核心内容

WKWebView 是 iOS app 中嵌入 Web 内容的标准组件。iOS 15 对它做了多项增强，覆盖了查找交互、JavaScript 互操作、内容安全和媒体能力四个方面。

Find Interaction 是最显眼的改进。WKWebView 现在原生支持了页面内搜索（Find on Page）功能，使用系统标准的 `UIFindInteraction` API。之前要实现这个功能需要自己写 JavaScript 注入方案，现在只需要设置 `isFindInteractionEnabled = true` 就能使用系统级的搜索 UI。

JavaScript 互操作方面，`WKWebView` 新增了 `callAsyncJavaScript` 方法，可以在一个调用中执行一段异步 JavaScript 并等待结果。之前要执行异步 JS 代码需要通过 message handler 回传结果，流程很繁琐。

媒体方面，WKWebView 现在支持了 `Media Capture` API，Web 页面可以直接访问摄像头和麦克风（在用户授权后）。PIcture in Picture 也得到了增强。


### 最佳实践

如果你的 app 需要支持页面搜索，`isFindInteractionEnabled` 是最简单的方案，但 UI 是系统固定的样式。如果需要自定义搜索 UI，可以用 `webView.find(_:configuration:completionHandler:)` 方法手动执行搜索，然后自己渲染搜索结果。

`callAsyncJavaScript` 的返回值类型映射遵循 JSON 规则：JS 的 `number` 映射为 `NSNumber`，`string` 映射为 `String`，`object` 映射为 `Dictionary`。如果你的 JS 返回了 `undefined`，Swift 端会收到 `nil`。

---

## 相关 Session

- [Accelerate networking with HTTP/3 and QUIC](/articles/wwdc2021-10094) — the web is changing, and the next major version of http is here. learn how http/
- [Adopt Quick Note](/articles/wwdc2021-10264) — learn how you can link your app to quick note and help people quickly connect yo
- [Develop advanced web content](/articles/wwdc2021-10030) — develop in javascript, webgl, or webassembly? learn how the latest updates to sa
- [Meet privacy-preserving ad attribution](/articles/wwdc2021-10033) — discover how you can measure your ad campaigns in apps and on the web without co
