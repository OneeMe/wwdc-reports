---
title: "Formatters: Make data human-friendly"
description: "save yourself time and frustration: when you display data in your app — including dates, times, measurements, names, lists, numbers, or strings — learn how to format it correctly and provide a great e"
date: 2026-06-10
tags: ["WWDC 2020"]
thumbnail: "/images/sessions/2020/10160.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2020/10160/"
sessionId: "wwdc2020-10160"
year: "2020"
relatedSessions:
  - title: "A conversation with Lisa Jackson and former Attorney General Eric Holder"
    code: "10970"
    description: "eric holder was the 82nd attorney general of the united states, having served fr"
  - title: "Build location-aware enterprise apps"
    code: "10140"
    description: "develop location-aware enterprise apps for your business and personalize your em"
  - title: "Create a seamless speech experience in your apps"
    code: "10022"
    description: "augment your app"
  - title: "Swan's Quest, Chapter 1: Voices in the dark"
    code: "10681"
    description: "swift playgrounds presents "
---

# Formatters: Make data human-friendly

---

## Highlight

> 深入了解 Formatters: Make data human-friendly 的技术细节和最佳实践。

---

## 核心内容

Formatter 是 Foundation 框架中负责将机器数据（日期、数字、度量值等）转换为人类可读文本的核心组件。iOS 14 引入了全新的 Formatter 体系，彻底重构了 `DateFormatter`、`NumberFormatter` 等老 API 的使用方式。新的 API 采用"配置式"设计——你创建一个 Formatter 实例，设置格式风格，然后直接格式化值，不再需要反复修改同一个实例的属性。

Session 的核心亮点是 `Date.Formatter` 和 `Number.Formatter` 的新便捷 API。你不再需要创建和管理 Formatter 实例，而是直接使用静态方法。比如 `date.formatted(date: .long, time: .short)` 就能得到一个符合用户区域设置的长日期+短时间字符串。这种方式不仅代码更简洁，性能也大幅提升——Apple 声称新 API 比 `DateFormatter` 快了 80 倍。

另一个重要的新增是 `ListFormatter` 和 `RelativeDateTimeFormatter` 的增强。`ListFormatter` 可以根据区域设置正确地格式化列表（中文用顿号、英文用牛津逗号），而 `RelativeDateTimeFormatter` 可以输出"3小时前""昨天"这样的相对时间描述。这些细节决定了你的应用在不同语言环境下是否显得"地道"。


### 最佳实践

- 不要再缓存 `DateFormatter` 实例——新的 `.formatted()` API 已经内置了高效缓存
- 始终使用 Formatter API 处理面向用户的文本，不要手动拼接日期和数字字符串
- 使用 `ListFormatter` 来格式化枚举列表，它能自动处理不同语言的列表分隔规则
- 测试时切换到阿拉伯语、希伯来语等 RTL 语言，确保 Formatter 正确处理文本方向
- 对于性能敏感的场景（如列表中格式化大量数据），可以预先创建一个 `FormatStyle` 实例复用

---

## 相关 Session

- [A conversation with Lisa Jackson and former Attorney General Eric Holder](/articles?year=2020&topic=all&search=10970) — eric holder was the 82nd attorney general of the united states, having served fr
- [Build location-aware enterprise apps](/articles?year=2020&topic=all&search=10140) — develop location-aware enterprise apps for your business and personalize your em
- [Create a seamless speech experience in your apps](/articles?year=2020&topic=all&search=10022) — augment your app
- [Swan's Quest, Chapter 1: Voices in the dark](/articles?year=2020&topic=all&search=10681) — swift playgrounds presents 
