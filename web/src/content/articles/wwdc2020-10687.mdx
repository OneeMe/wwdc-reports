---
title: "Triage test failures with XCTIssue"
description: "put your test failures to work: learn how to triage and diagnose uncaught issues in your app using the latest testing apis in xcode. we"
date: 2026-06-10
tags: ["WWDC 2020"]
thumbnail: "/images/sessions/2020/10687.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2020/10687/"
sessionId: "wwdc2020-10687"
year: "2020"
relatedSessions:
  - title: "App essentials in SwiftUI"
    code: "10037"
    description: "thanks to the new app protocol, swiftui now supports building entire apps! see h"
  - title: "Build a SwiftUI view in Swift Playgrounds"
    code: "10643"
    description: "easily prototype and play around with swiftui views when you use them with swift"
  - title: "Build document-based apps in SwiftUI"
    code: "10039"
    description: "learn how to build a document-based app entirely in swiftui! we"
  - title: "Control training in Create ML with Swift"
    code: "10156"
    description: "with the create ml framework you have more power than ever to easily develop mod"
---

## Highlight

> Xcode 12 引入了 `XCTIssue` 类型来替代之前基于 `NSError` 的测试失败报告机制。以前 `XCTestObservation` 的回调只给你一个 error 对象，你只能拿到错误描述字符串。现在每个测试失败都是一个 `XCTIssue` 实例，包含类型、描述、源码位置、附件（截图/文件）等结构化信息。

---

## 核心内容

Xcode 12 引入了 `XCTIssue` 类型来替代之前基于 `NSError` 的测试失败报告机制。以前 `XCTestObservation` 的回调只给你一个 error 对象，你只能拿到错误描述字符串。现在每个测试失败都是一个 `XCTIssue` 实例，包含类型、描述、源码位置、附件（截图/文件）等结构化信息。

`XCTIssue` 的类型枚举：
- `.assertionFailure`：`XCTAssertEqual` 等断言失败
- `.performanceRegression`：性能测试的指标回归
- `.uncaughtException`：测试中未捕获的异常
- `.system`：系统级问题（超时、崩溃）
- `.failure`：通过 `XCTFail()` 显式标记的失败

Session 还介绍了如何用 `XCTestObservation` 协议的新方法 `testSuite(_:didRecord:)` 来捕获 `XCTIssue`，在 CI 中实现自定义的报告和通知。


### 最佳实践

1. **CI 中用 `XCTIssue.type` 分类失败**。断言失败和系统超时应该走不同的告警通道。断言失败直接通知开发者，系统超时先检查 CI 环境。
2. **自定义断言时主动添加附件**。比如 UI 测试失败时附加截图，网络测试失败时附加 response body。这样在 CI 报告中可以直接看到上下文，不用回本地复现。
3. **迁移旧代码中的 `XCTestObservation` 方法**。把 `didFailWithDescription` 替换为 `didRecord`，旧的接口在未来版本中会被移除。

---

## 相关 Session

- [App essentials in SwiftUI](/articles/wwdc2020-10037) — thanks to the new app protocol, swiftui now supports building entire apps! see h
- [Build a SwiftUI view in Swift Playgrounds](/articles/wwdc2020-10643) — easily prototype and play around with swiftui views when you use them with swift
- [Build document-based apps in SwiftUI](/articles/wwdc2020-10039) — learn how to build a document-based app entirely in swiftui! we
- [Control training in Create ML with Swift](/articles/wwdc2020-10156) — with the create ml framework you have more power than ever to easily develop mod
