---
title: "Create high quality images using Image Playground"
description: "enable high-quality image creation in your app using image playground. with a new generative model that runs on private cloud compute, users can make images in virtually any style, including photoreal"
date: 2026-06-10
tags: ["ImagePlayground", "PrivateCloudCompute", "SwiftUI", "GenerativeAI"]
thumbnail: "/images/sessions/2026/375.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/375/"
sessionId: "wwdc2026-375"
year: "2026"
relatedSessions:
  - title: "Bring an LLM provider to the Foundation Models framework"
    code: "339"
    description: "extend the foundation models framework by implementing a languagemodelexecutor f"
  - title: "Bringing Cyberpunk 2077 to Mac"
    code: "356"
    description: "go behind the scenes and learn from cd projekt red how cyberpunk 2077 came to ma"
  - title: "Build agentic app experiences with the Foundation Models framework"
    code: "242"
    description: "learn how to take your intelligence features further with foundation models fram"
  - title: "Build AI-powered scripts with the fm CLI and Python SDK"
    code: "334"
    description: "explore all the new ways to leverage apple foundation models on macos. the found"
---

## Highlight

> 深入了解 Create high quality images using Image Playground 的技术细节和最佳实践。

---

## 核心内容

这场 Session 最值得关注的一件事是：Apple 正式把 Image Playground (图像游乐场) 的底层模型迁移到了 Private Cloud Compute (私有云计算，简称 PCC)，并顺势废弃了允许后台静默生图的 `ImageCreator` API。以前你想在 App 里加个 AI 画图功能，得自己搞定 Midjourney 或 Stable Diffusion 的 API 鉴权、后端代理、按量计费，还得自己手写一套 Prompt 输入和风格选择的 UI。现在 Apple 直接扔给你一个原生的 Sheet (表单)，底层算力走 PCC，隐私拉满，你只需要把 App 里的上下文丢进去，然后接收生成的图片 URL 就行了。

这对所有想在 App 里蹭 AI 生图热度的开发者影响巨大。你不再需要维护昂贵的 GPU 服务器，也不用担心用户乱发 Prompt 导致你的 API 账号被封。代价是你失去了对生图过程的绝对控制权，必须接受 Apple 规定的交互范式：用户必须在系统提供的 Sheet 里确认和微调，App 不能躲在后台偷偷生成。


### 最佳实践

如果你的老项目还在用 `ImageCreator` 在后台偷偷生图，赶紧停下。现在的迁移策略是：把生图入口改造成一个显眼的按钮，点击后弹出 `.imagePlaygroundSheet`。把原来拼接 Prompt 的逻辑，拆解成 `ImagePlaygroundConcept` 的 `.text` 和 `.extracted`。

新项目强烈建议把 Image Playground 作为“用户赋能”工具，而不是“自动化”工具。比如在一个日记 App 里，不要让用户一打开日记就自动在后台生成四张配图（这会触发 PCC 限制且体验突兀），而是提供一个“为这篇日记画个插图”的按钮，并把日记正文通过 `.extracted` 传给 Sheet。

实战中最容易踩的坑是忽略了 iCloud+ 的限制。Image Playground 依赖 PCC，而 PCC 的配额是跟用户的 iCloud+ 订阅绑定的。你不需要自己写 UI 去提示用户“额度不足”，系统 Sheet 会自己处理这些恶心的边界情况，这也是 Apple 强制使用系统 UI 的原因之一。

---

## 相关 Session

- [Bring an LLM provider to the Foundation Models framework](/articles/wwdc2026-339) — extend the foundation models framework by implementing a languagemodelexecutor f
- [Bringing Cyberpunk 2077 to Mac](/articles/wwdc2026-356) — go behind the scenes and learn from cd projekt red how cyberpunk 2077 came to ma
- [Build agentic app experiences with the Foundation Models framework](/articles/wwdc2026-242) — learn how to take your intelligence features further with foundation models fram
- [Build AI-powered scripts with the fm CLI and Python SDK](/articles/wwdc2026-334) — explore all the new ways to leverage apple foundation models on macos. the found
