---
title: "Build a responsive camera app that launches quickly"
description: "discover how to build a camera app that launches instantly so people never miss the perfect shot. explore how to optimize the entire camera launch sequence — from app startup to first preview frame. e"
date: 2026-06-10
tags: ["AVFoundation", "AVCaptureSession", "Deferred Start", "AVProVideoStorage", "Camera"]
thumbnail: "/images/sessions/2026/303.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/303/"
sessionId: "wwdc2026-303"
year: "2026"
relatedSessions:
  - title: "Meet the Now Playing framework"
    code: "312"
    description: "get a first look at now playing — a swift framework that connects your app's med"
  - title: "Prepare your tvOS apps for Dynamic Type"
    code: "221"
    description: "dynamic type empowers people to comfortably read and interact with your app by l"
---

# Build a responsive camera app that launches quickly

---

## Highlight

> 做相机或扫码 App 的兄弟肯定懂，`AVCaptureSession` 启动时初始化那些沉重的 `AVCaptureOutput`（比如 `AVCapturePhotoOutput`）极其耗时，导致首帧预览慢半拍，用户经常错过精彩瞬间。Apple 这次直接把启动流程拆成了两半：预览层先跑起来，拍照和录像的 Output 延后初始化。

---

## 核心内容

做相机或扫码 App 的兄弟肯定懂，`AVCaptureSession` 启动时初始化那些沉重的 `AVCaptureOutput`（比如 `AVCapturePhotoOutput`）极其耗时，导致首帧预览慢半拍，用户经常错过精彩瞬间。Apple 这次直接把启动流程拆成了两半：预览层先跑起来，拍照和录像的 Output 延后初始化。

配合全新的 `AVProVideoStorage` API，高码率视频（像 ProRes）写入时的 I/O 抖动问题也迎来了终结。这套组合拳对重度依赖相机的 App（社交、直播、扫码、专业摄影）影响巨大，不仅能显著降低启动耗时，还能彻底消灭录制高规格视频时的掉帧隐患。


### 最佳实践

已有项目迁移时，最快的方式是直接升级到 iOS 26 SDK 并重新编译。Apple 把 `automaticallyRunsDeferredStart` 默认设为了 true，只要你没有写死复杂的 Session 状态机，通常能白嫖到启动速度的提升。

新项目建议直接采用手动模式（Manual Deferred Start）。把 UI 也分两阶段：第一阶段只渲染取景框和快门，第二阶段（首帧上屏后）再淡入相册缩略图、滤镜选择器等重型 UI。

实战中最容易踩的坑是主线程阻塞。`AVCaptureSession` 的 `startRunning()` 和 `commitConfiguration()` 依然是同步阻塞调用。千万别在 `viewDidLoad` 里直接调，老老实实 dispatch 到专门的 `sessionQueue` 串行队列里执行。

---

## 相关 Session

- [Meet the Now Playing framework](/articles?year=2026&topic=all&search=312) — get a first look at now playing — a swift framework that connects your app's med
- [Prepare your tvOS apps for Dynamic Type](/articles?year=2026&topic=all&search=221) — dynamic type empowers people to comfortably read and interact with your app by l
