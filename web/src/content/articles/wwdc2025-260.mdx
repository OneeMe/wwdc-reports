---
title: "Develop for Shortcuts and Spotlight with App Intents"
description: "learn about how building app intents that make actions available and work best with the new features in shortcuts and spotlight on mac. we"
date: 2026-06-10
tags: ["机器学习"]
thumbnail: "/images/sessions/2025/260.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2025/260/"
sessionId: "wwdc2025-260"
year: "2025"
relatedSessions:
  - title: "Bring advanced speech-to-text to your app with SpeechAnalyzer"
    code: "277"
    description: "discover the new speechanalyzer api for speech to text. we"
  - title: "Code-along: Bring on-device AI to your app using the Foundation Models framework"
    code: "259"
    description: "develop generative ai features for your swiftui apps using the foundation models"
  - title: "Deep dive into the Foundation Models framework"
    code: "301"
    description: "level up with the foundation models framework. learn how guided generation works"
  - title: "Design interactive snippets"
    code: "281"
    description: "snippets are compact views invoked from app intents that display information fro"
---

## Highlight

> 三个板块：Use Model action、Spotlight on Mac、Automations on Mac。

---

## 核心内容

三个板块：Use Model action、Spotlight on Mac、Automations on Mac。

**Use Model action**：用户可以选择 Private Cloud Compute 服务器模型、设备端模型或 ChatGPT。模型输出可自动推断类型（比如 If action 需要 Boolean，模型会自动返回 yes/no）。支持三种输出类型——Text（支持富文本 Attributed String）、Dictionary（结构化数据提取）、App Entity（过滤/转换实体列表）。支持 Follow Up 功能，用户可以在初始请求后与模型来回对话调整输出。

**App Entity 与模型的交互**：传入实体列表给模型时，实体会被序列化为 JSON。序列化包含三部分：所有 Shortcuts 暴露的属性（转为字符串）、Type Display Representation 的名称、Display Representation 的 title 和 subtitle。这意味着实体暴露的信息量直接决定了模型推理的质量。

**Spotlight on Mac**：App Intents 现在可以直接在 Spotlight 中运行。要求：参数摘要必须包含所有无默认值的 required 参数、intent 不能被标记为隐藏。支持建议填充（Suggested Entities / All Entities）、搜索查询（Entity String Query / Indexed Entity）、前台/后台执行模式分离（通过 Opens Intent 关联）。

**Automations on Mac**：个人自动化带到 Mac，新增文件夹和外置硬盘自动化类型。只要 intent 在 macOS 可用，就能用于自动化。


### 最佳实践

1. **重新审视你的 Entity 属性**。从"模型能理解什么"的角度审查每个属性——属性名应该自解释，属性值应该是人类可读的字符串。模型看到的是 JSON 序列化结果，不是你的代码。

2. **富文本参数用 AttributedString**。如果 app 支持富文本编辑，intent 的文本参数必须用 AttributedString 类型，否则模型输出的格式会丢失。

3. **参数摘要包含所有必要信息**。Spotlight 依赖参数摘要展示 intent——所有无默认值的 required 参数都必须出现在摘要中，否则 intent 不会在 Spotlight 中显示。

4. **为每个参数提供 Suggestions**。大列表用 `suggestedEntities`（比如未来一天的日历事件），有限集合用 `allEntities`（比如时区列表）。没有建议的参数字段在 Spotlight 中体验很差。

5. **考虑 Follow Up 对 workflow 的影响**。Use Model action 的 Follow Up 功能让用户可以在模型输出后继续对话——这意味着你的 intent 需要能处理比预期更丰富或格式不同的输入。

---

## 相关 Session

- [Bring advanced speech-to-text to your app with SpeechAnalyzer](/articles/wwdc2025-277) — discover the new speechanalyzer api for speech to text. we
- [Code-along: Bring on-device AI to your app using the Foundation Models framework](/articles/wwdc2025-259) — develop generative ai features for your swiftui apps using the foundation models
- [Deep dive into the Foundation Models framework](/articles/wwdc2025-301) — level up with the foundation models framework. learn how guided generation works
- [Design interactive snippets](/articles/wwdc2025-281) — snippets are compact views invoked from app intents that display information fro
