---
title: "Discover container machines"
description: "meet container machines, a new tool included in container that offers a lightweight persistent linux environment on mac. explore how container machines work and how the design of containerization allo"
date: 2026-06-10
tags: ["Containerization", "Linux", "Swift Server", "CLI", "Virtualization"]
thumbnail: "/images/sessions/2026/389.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/389/"
sessionId: "wwdc2026-389"
year: "2026"
relatedSessions:
  - title: "Build, deliver, and automate with Xcode Cloud"
    code: "261"
    description: "discover the latest updates to xcode cloud that quickly get you started building"
  - title: "Code-along: Add persistence with SwiftData"
    code: "275"
    description: "experience swiftdata in action as we add persistence to an existing app. we'll s"
  - title: "Get the most out of Device Hub"
    code: "260"
    description: "learn how device hub can accelerate your development workflows. we'll take a tou"
  - title: "Integrate MusicKit into your app"
    code: "254"
    description: "bring the power of apple music into your app using musickit. we'll cover authori"
---

## Highlight

> 做 Swift 服务端或者跨平台 C++ 开发的同事都知道，在 Mac 上搞 Linux 环境一直是个痛点。Docker Desktop 越来越重，传统虚拟机启动慢且文件共享卡顿。Apple 去年开源了 `Containerization` (容器化) 框架，今年直接端出了 `Container machine`。

---

## 核心内容

做 Swift 服务端或者跨平台 C++ 开发的同事都知道，在 Mac 上搞 Linux 环境一直是个痛点。Docker Desktop 越来越重，传统虚拟机启动慢且文件共享卡顿。Apple 去年开源了 `Containerization` (容器化) 框架，今年直接端出了 `Container machine`。

它不是普通的 ephemeral (无状态) 容器，而是一个跑在轻量级 VM 里、支持 OCI 镜像、且**有状态**的 Linux 环境。最爽的是它自动映射了你的 macOS 用户名和当前工作目录。你在 Mac 上用 Xcode 改完代码，切到终端敲个 `container machine run swift build`，它直接读取本地文件并在 Linux 里编译，体验就像在跑原生 macOS 程序一样丝滑。这对 Swift 服务端开发者、Rust/C++ 跨平台工程师来说，是实打实的生产力解放。


### 最佳实践

如果你现在还在用 Docker Desktop 跑本地 Linux 开发环境，建议立刻把日常编译和测试流程迁移到 `Container machine`。它的亚秒级启动和原生文件系统共享能省下大量等待时间。

对于新项目，推荐把 `Container machine` 作为跨平台 CI/CD 的本地验证基石。在 Mac 上写完代码，用 `container machine run swift test` 跑一遍 Linux 测试，再推代码。

实战中最容易踩的坑是**网络隔离**。Demo 里特意提到，为了让 Mac 上的 Safari 访问 Linux 里的 Vapor 服务，需要把 Vapor 的 hostname 绑定到 `Container machine` 的特定 IP（通过 `container machine list` 获取），而不是默认的 `127.0.0.1`。在配置本地服务端口映射时，务必先查清楚虚拟网卡的 IP 分配规则。

---

## 相关 Session

- [Build, deliver, and automate with Xcode Cloud](/articles/wwdc2026-261) — discover the latest updates to xcode cloud that quickly get you started building
- [Code-along: Add persistence with SwiftData](/articles/wwdc2026-275) — experience swiftdata in action as we add persistence to an existing app. we'll s
- [Get the most out of Device Hub](/articles/wwdc2026-260) — learn how device hub can accelerate your development workflows. we'll take a tou
- [Integrate MusicKit into your app](/articles/wwdc2026-254) — bring the power of apple music into your app using musickit. we'll cover authori
