---
title: "Dive into lazy stacks and scrolling with SwiftUI"
description: "discover the inner workings of lazy stacks in swiftui. we'll explore how lazyvstack and lazyhstack estimate sizes, lazily load subviews, and prefetch content to deliver smooth scrolling experiences. w"
date: 2026-06-10
tags: ["SwiftUI", "LazyVStack", "ScrollView", "Performance", "Layout"]
thumbnail: "/images/sessions/2026/321.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/321/"
sessionId: "wwdc2026-321"
year: "2026"
relatedSessions:
  - title: "Communicate your brand identity on iOS"
    code: "251"
    description: "explore practical approaches to branding using typography, color, components, an"
  - title: "Compose advanced graphics effects with SwiftUI"
    code: "322"
    description: "discover how to craft rich, custom experiences by creatively composing swiftui l"
  - title: "Craft clear names for features and labels in your app"
    code: "290"
    description: "the names of your products, features, and labels can determine how people unders"
  - title: "Learn CSS Grid Lanes"
    code: "314"
    description: "build adaptive web layouts that embrace content of all shapes and sizes. explore"
---

# Dive into lazy stacks and scrolling with SwiftUI

---

## Highlight

> 平时我们用 `LazyVStack` 和 `LazyHStack` 觉得挺省事，不用像 `List` 那样受限于系统样式。但一遇到复杂信息流、动态高度或者需要代码控制滚动位置时，经常遇到滚动条乱跳、快速滑动掉帧的玄学问题。

---

## 核心内容

平时我们用 `LazyVStack` 和 `LazyHStack` 觉得挺省事，不用像 `List` 那样受限于系统样式。但一遇到复杂信息流、动态高度或者需要代码控制滚动位置时，经常遇到滚动条乱跳、快速滑动掉帧的玄学问题。

这次 Apple 把底牌亮了：Lazy Stack 根本不计算所有子视图的大小，它靠“猜”（Estimation）。它只渲染屏幕内的视图，屏幕外的高度是基于已渲染视图的平均值估算出来的。同时，为了避免快速滑动卡顿，它引入了预取（Prefetching）机制，在视图上屏前提前做部分渲染。

这对做图文信息流、电商商品列表或者任何需要精准控制滚动的开发者影响最大。搞懂了这套机制，你就能明白为什么你的列表越滚越卡，以及怎么通过调整状态管理来榨干 SwiftUI 的性能。


### 最佳实践

已有项目迁移时，第一步是全局搜索 `LazyVStack` 和 `LazyHStack` 内部的 `onAppear`。把里面的数据加载、图片解码、复杂计算全部抽离到 ViewModel 的 `init` 或异步任务中。第二步，检查 `ForEach` 内部是否存在 `if/else` 条件渲染。如果有，尽量将条件判断上移到数据层，在传入 `ForEach` 前就过滤好数据，保证 `ForEach` 内部是 1 对 1 的稳定视图映射。

新项目采用时，牢记 Lazy Stack 的“首项决定论”。对于 `LazyHStack`，它的高度由第一个子视图决定；对于 `LazyVStack`，它的宽度由第一个子视图决定。在设计 UI 时，确保第一个渲染的视图具有代表性的高度或宽度，或者直接给容器和子视图指定明确的 frame，避免后续视图因尺寸不一被意外裁切。

---

## 相关 Session

- [Communicate your brand identity on iOS](/articles?year=2026&topic=all&search=251) — explore practical approaches to branding using typography, color, components, an
- [Compose advanced graphics effects with SwiftUI](/articles?year=2026&topic=all&search=322) — discover how to craft rich, custom experiences by creatively composing swiftui l
- [Craft clear names for features and labels in your app](/articles?year=2026&topic=all&search=290) — the names of your products, features, and labels can determine how people unders
- [Learn CSS Grid Lanes](/articles?year=2026&topic=all&search=314) — build adaptive web layouts that embrace content of all shapes and sizes. explore
