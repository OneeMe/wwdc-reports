---
title: "What’s new in notarization for Mac apps"
description: "notarization works in tandem with macos to help people safely download software for their mac outside of the app store. learn about the required transition from altool to notarytool and how the xcode "
date: 2026-06-10
tags: ["WWDC 2022"]
thumbnail: "/images/sessions/2022/10109.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2022/10109/"
sessionId: "wwdc2022-10109"
year: "2022"
relatedSessions:
  - title: "Discover Managed Device Attestation"
    code: "10143"
    description: "learn how to use managed device attestation to ensure only legitimate devices ca"
  - title: "Get to know Developer Mode"
    code: "110344"
    description: "meet developer mode — required on ios 16, ipados 16, and watchos 9 to install, r"
  - title: "Improve DNS security for apps and servers"
    code: "10079"
    description: "discover the latest ways to ensure that dns — the foundation of internet address"
  - title: "Replace CAPTCHAs with Private Access Tokens"
    code: "10077"
    description: "don"
---

# What’s new in notarization for Mac apps

---

## Highlight

> 这场 Session 围绕 macOS App 公证（notarization）流程的三个层面讲了变化：工具链更新、API 现代化、以及工作流集成。

---

## 核心内容

这场 Session 围绕 macOS App 公证（notarization）流程的三个层面讲了变化：工具链更新、API 现代化、以及工作流集成。

**altool 退役。** 用了多年的 `altool --notarize-app` 命令将在 2023 年秋天正式退役。替代方案有两个：Xcode 14 的内置公证流程和新的 REST API（notarytool）。如果你还在用 altool 的 CI 脚本，现在就该迁移了。

**Xcode 14 内置公证提速。** Xcode 14 把公证流程集成到了 Archive 流程里。你只需要在 Organizer 里点击 "Distribute App"，选择公证选项，Xcode 会自动完成签名、上传、等待审核、装订（stapling）整个流程。Apple 声称速度提升了 4 倍——从提交到拿到结果，之前可能要等几分钟甚至十几分钟，现在通常不到一分钟。

**新的 REST API（notarytool）。** 这是给 CI/CD 管线用的。`notarytool` 是一个命令行工具（Xcode 自带），底层调用 Apple 的 Notary API。它支持 JWT（JSON Web Token）认证，不再需要 App-specific password。认证流程是：用你的 Apple ID 生成一个 JWT token，然后用这个 token 调用 API。

**S3 上传。** 新的 Notary API 支持直接上传到 Apple 提供的 S3 bucket。之前的流程是把整个 App 压缩包上传到 Apple 的服务器，对于大型 App 来说上传很慢。新的流程是先调用 API 获取一个预签名的 S3 URL，然后用这个 URL 上传到 S3。上传完成后通知 Apple 开始审核。

**Webhook 支持。** 之前提交公证后需要轮询 API 检查状态，现在可以注册 webhook URL，Apple 审核完成后会主动 POST 通知你。这对 CI/CD 管线来说是一个巨大的改进——不再需要写 `while true; sleep 30; notarytool check` 这种轮询脚本了。


### 最佳实践

迁移 CI 脚本时，优先使用 JWT 认证（API key 方式）而不是 Apple ID + App-specific password。JWT 方式不需要 2FA 交互，更适合自动化环境。而且 API key 不会过期（除非你主动撤销），比 App-specific password 更稳定。

对于大型 App（超过 200MB），使用 S3 上传方式。notarytool 默认会用 S3 上传，你不需要额外配置。如果你的 CI 环境网络到 Apple S3 bucket 的延迟很高，可以考虑用 `--s3-accelerate` 参数启用加速上传。

装订（stapling）是必须的步骤。公证通过后一定要跑 `xcrun stapler staple`，否则用户第一次打开 App 时需要联网验证。装订会把公证票据附加到 App 包里，让 macOS 可以离线验证。

---

## 相关 Session

- [Discover Managed Device Attestation](/articles?year=2022&topic=all&search=10143) — learn how to use managed device attestation to ensure only legitimate devices ca
- [Get to know Developer Mode](/articles?year=2022&topic=all&search=110344) — meet developer mode — required on ios 16, ipados 16, and watchos 9 to install, r
- [Improve DNS security for apps and servers](/articles?year=2022&topic=all&search=10079) — discover the latest ways to ensure that dns — the foundation of internet address
- [Replace CAPTCHAs with Private Access Tokens](/articles?year=2022&topic=all&search=10077) — don
