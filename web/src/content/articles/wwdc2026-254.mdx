---
title: "Integrate MusicKit into your app"
description: "bring the power of apple music into your app using musickit. we'll cover authorization, subscription-status checks, music selection, playback control, and cross-storefront song sharing. learn how to u"
date: 2026-06-10
tags: ["MusicKit", "SwiftUI", "SystemMusicPlayer", "MusicPicker", "Apple Music"]
thumbnail: "/images/sessions/2026/254.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/254/"
sessionId: "wwdc2026-254"
year: "2026"
relatedSessions:
  - title: "Build, deliver, and automate with Xcode Cloud"
    code: "261"
    description: "discover the latest updates to xcode cloud that quickly get you started building"
  - title: "Code-along: Add persistence with SwiftData"
    code: "275"
    description: "experience swiftdata in action as we add persistence to an existing app. we'll s"
  - title: "Discover container machines"
    code: "389"
    description: "meet container machines, a new tool included in container that offers a lightwei"
  - title: "Get the most out of Device Hub"
    code: "260"
    description: "learn how device hub can accelerate your development workflows. we'll take a tou"
---

# Integrate MusicKit into your app

---

## Highlight

> 深入了解 Integrate MusicKit into your app 的技术细节和最佳实践。

---

## 核心内容

这场 Session 最值得关注的一件事是：Apple 把 MusicKit 从一个“给纯音乐 App 用的底层 API”变成了“任何 App 都能轻松调用的 SwiftUI 插件”。以前你要在 App 里加个“健身时听 Apple Music”的功能，得自己写授权逻辑、判断订阅状态、手搓选歌列表、处理播放队列，现在全被 `.musicPicker` 和 `.musicSubscriptionOffer` 这两个 View Modifier 包圆了。

Apple 这次明确划分了 `SystemMusicPlayer`（系统播放器）和 `ApplicationMusicPlayer`（应用内播放器）的边界，并补齐了异步状态监听的短板。这对健身、冥想、游戏或者社交类 App 影响最大，你不再需要为了加个 BGM 功能去重写半个 App，几行代码就能拉起原生的选歌和订阅 UI，甚至还能通过 Apple Services Performance Partner Program 拿订阅佣金。


### 最佳实践

对于已有项目，如果你之前用 `MPMediaPickerController` 或者自己写了一套 Apple Music API 的请求和选歌逻辑，强烈建议迁移到 `.musicPicker`。这能帮你砍掉成百上千行的 UI 和数据解析代码，把精力放回业务本身。

新项目接入时，优先明确你的 App 是“音乐播放器”还是“需要 BGM 的工具”。如果是后者（比如健身、番茄钟），无脑选择 `ApplicationMusicPlayer`，自己掌控队列和播放时机；如果是前者（比如独立的音乐客户端），使用 `SystemMusicPlayer` 能帮你省去处理锁屏控制中心和后台音频中断的麻烦。

实战中最容易踩的坑是网络延迟导致的播放卡顿。在构建播放队列时，尽量使用容器类型（如 `Album` 或 `Playlist`）的特殊队列初始化方法，MusicKit 会在底层做延迟加载（Lazy loading），这比把几百首 `Song` 塞进数组里再传给 Player 要快得多。

---

## 相关 Session

- [Build, deliver, and automate with Xcode Cloud](/articles?year=2026&topic=all&search=261) — discover the latest updates to xcode cloud that quickly get you started building
- [Code-along: Add persistence with SwiftData](/articles?year=2026&topic=all&search=275) — experience swiftdata in action as we add persistence to an existing app. we'll s
- [Discover container machines](/articles?year=2026&topic=all&search=389) — meet container machines, a new tool included in container that offers a lightwei
- [Get the most out of Device Hub](/articles?year=2026&topic=all&search=260) — learn how device hub can accelerate your development workflows. we'll take a tou
