---
title: "Explore spatial accessory input on visionOS"
description: "learn how you can integrate spatial accessories into your app. display virtual content, interact with your app, track them in space, and get information on interactions for enhanced virtual experience"
date: 2026-06-10
tags: ["图形与游戏", "空间计算"]
thumbnail: "/images/sessions/2025/289.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2025/289/"
sessionId: "wwdc2025-289"
year: "2025"
relatedSessions:
  - title: "Bring your SceneKit project to RealityKit"
    code: "288"
    description: "understand scenekit deprecation and explore how to transition your 3d projects t"
  - title: "Combine Metal 4 machine learning and graphics"
    code: "262"
    description: "learn how to seamlessly combine machine learning into your graphics applications"
  - title: "Discover Metal 4"
    code: "205"
    description: "learn how to get started leveraging the powerful new features of metal 4 in your"
  - title: "Engage players with the Apple Games app"
    code: "215"
    description: "meet the games app – a new destination for players to keep up with what"
---

# Explore spatial accessory input on visionOS

---

## Highlight

> Amanda Han 用一个数字雕刻应用演示了空间配件的完整开发流程。

---

## 核心内容

Amanda Han 用一个数字雕刻应用演示了空间配件的完整开发流程。

**支持的配件：** PS VR2 Sense 控制器（按钮、摇杆、扳机，适合游戏）和 Logitech Muse（压感笔尖、侧键、触觉反馈，适合生产力和创意应用）。两者都可以在 shared space 和 full space 中使用。

**连接管理走 GameController 框架。** 通过 `GCStylusDidConnect`/`GCControllerDidConnect` 通知发现配件，检查 `productCategory` 确认是否支持空间追踪（`.spatialStylus` 或 `.spatialController`）。需要在 plist 中声明 `Spatial Gamepad` capability。

**锚定用 RealityKit AnchorEntity。** 创建 `AccessoryAnchoringSource`，指定追踪位置（如 `aim`），然后构建 `AnchorEntity`。追踪模式有两种：`.predicted` 用预测模型降低延迟（适合渲染和快速交互，但剧烈运动可能过冲）和 `.continuous` 更精确但延迟更高。

**SpatialTrackingSession 新增 `.accessory` 配置。** 启用后就能在 RealityKit 中获取 accessory anchor 的 transform 数据。

**触觉反馈通过 haptics engine 实现。** 从配件获取 haptics 对象，创建 engine，启动即可。在雕刻场景中，每切掉一块虚拟粘土就播放一段触觉反馈。

**ARKit AccessoryAnchor 提供更丰富的信息：** 手持方向（handedness/chirality）、相对运动、旋转变化、追踪质量。可以从 RealityKit AnchorEntity 通过 `ARKitAnchorComponent` 桥接到 ARKit anchor。


### 最佳实践

**设计自适应输入。** 手和眼仍然是 visionOS 的核心输入方式，配件是增强而非替代。App 应同时支持 hands 和 spatial accessories，用 App Store 上的 "Spatial game controller support" badge 标识。

**配件断连时优雅处理。** 用 `GCStylusDidDisconnect` 通知重建 ARKit session，移除锚定的虚拟内容，避免残留幽灵对象。

**full space app 中隐藏系统 UI。** 配合 `.persistentSystemOverlays(.hidden)` 隐藏 home indicator，用 `.upperLimbVisibility(.hidden)` 隐藏上肢和配件模型，增强沉浸感。

**Predicted 模式下避免测量类操作。** 预测会引入偏差，如果需要精确坐标（如 CAD 应用），用 Continuous 模式或 ARKit 的 metric anchor API。

---

## 相关 Session

- [Bring your SceneKit project to RealityKit](/articles?year=2025&topic=all&search=288) — understand scenekit deprecation and explore how to transition your 3d projects t
- [Combine Metal 4 machine learning and graphics](/articles?year=2025&topic=all&search=262) — learn how to seamlessly combine machine learning into your graphics applications
- [Discover Metal 4](/articles?year=2025&topic=all&search=205) — learn how to get started leveraging the powerful new features of metal 4 in your
- [Engage players with the Apple Games app](/articles?year=2025&topic=all&search=215) — meet the games app – a new destination for players to keep up with what
