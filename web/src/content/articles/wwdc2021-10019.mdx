---
title: "Discover concurrency in SwiftUI"
description: "discover how you can use swift"
date: 2026-06-10
tags: ["WWDC 2021"]
thumbnail: "/images/sessions/2021/10019.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2021/10019/"
sessionId: "wwdc2021-10019"
year: "2021"
relatedSessions:
  - title: "ARC in Swift: Basics and beyond"
    code: "10216"
    description: "learn about the basics of object lifetimes and arc in swift. dive deep into what"
  - title: "Bring Core Data concurrency to Swift and SwiftUI"
    code: "10017"
    description: "discover how core data is adopting the new concurrency capabilities of swift 5.5"
  - title: "Explore structured concurrency in Swift"
    code: "10134"
    description: "when you have code that needs to run at the same time as other code, it"
  - title: "Meet AsyncSequence"
    code: "10058"
    description: "iterating over a sequence of values over time is now as easy as writing a “for” "
---

# Discover concurrency in SwiftUI

---

## Highlight

> Swift Concurrency（async/await、Actor、Task）在 WWDC 2021 正式发布，而这场 Session 专门讲解 SwiftUI 如何与这套并发模型整合。核心内容是三个新 API：`.task` 修饰符、`@MainActor` 在视图中的自动传播、以及 `AsyncStream` 作为 Timer 和 Delegate 的替代方案。

---

## 核心内容

Swift Concurrency（async/await、Actor、Task）在 WWDC 2021 正式发布，而这场 Session 专门讲解 SwiftUI 如何与这套并发模型整合。核心内容是三个新 API：`.task` 修饰符、`@MainActor` 在视图中的自动传播、以及 `AsyncStream` 作为 Timer 和 Delegate 的替代方案。

Session 用一个天气 app 作为贯穿示例。启动时加载天气数据、定时刷新、用户拖动地图时取消旧请求并发起新请求——这些场景用旧版的 `DispatchQueue` + `URLSession` 回调写起来很痛苦，但用 `.task` + `async let` 写起来就像同步代码一样直觉。

特别强调了取消语义。SwiftUI 视图消失时，`.task` 中的异步操作会被自动取消。如果你的网络请求不支持 cooperative cancellation，取消操作不会真正停止网络传输，只是不再处理结果。Session 建议在自定义的 async 函数中检查 `Task.isCancelled`，或者使用 `withTaskCancellationHandler` 来注册清理逻辑。


### 最佳实践

不要在 `.task` 中做无限循环的网络轮询。如果你的 app 需要实时数据更新，用 WebSocket 或者 `AsyncStream` + `Timer` 的组合，而不是在 `.task` 里写 `while true { await ... }`。无限循环会阻止 SwiftUI 在视图消失时正确清理资源。

对于复杂的数据加载逻辑，建议把异步操作封装到 ViewModel 或 Service 层，视图层只调用一个 async 方法。这样方便单元测试，也避免了视图代码膨胀。

---

## 相关 Session

- [ARC in Swift: Basics and beyond](/articles?year=2021&topic=all&search=10216) — learn about the basics of object lifetimes and arc in swift. dive deep into what
- [Bring Core Data concurrency to Swift and SwiftUI](/articles?year=2021&topic=all&search=10017) — discover how core data is adopting the new concurrency capabilities of swift 5.5
- [Explore structured concurrency in Swift](/articles?year=2021&topic=all&search=10134) — when you have code that needs to run at the same time as other code, it
- [Meet AsyncSequence](/articles?year=2021&topic=all&search=10058) — iterating over a sequence of values over time is now as easy as writing a “for” 
