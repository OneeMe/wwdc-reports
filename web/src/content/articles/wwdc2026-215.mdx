---
title: "Get started with the HTML Model Element"
description: "learn how the model element brings interactive 3d content to your websites — now on ios, ipados, macos, and visionos. discover tools for creating and optimizing 3d assets. explore model element's feat"
date: 2026-06-10
tags: ["HTML Model Element", "USDZ", "visionOS", "WebAR", "Spatial Computing"]
thumbnail: "/images/sessions/2026/215.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/215/"
sessionId: "wwdc2026-215"
year: "2026"
relatedSessions:
  - title: "Build live production tools for Apple Immersive Video"
    code: "338"
    description: "go behind the scenes of live apple immersive video production. discover how to p"
  - title: "Discover USDKit and what’s new in OpenUSD"
    code: "285"
    description: "dive into the latest advances in universal scene description (usd) support on ap"
  - title: "Explore immersive website environments in visionOS"
    code: "320"
    description: "transport your website's visitors into virtual environments in apple vision pro "
  - title: "visionOS Group Lab"
    code: "8004"
    description: "join us online for a deep dive into wwdc26 with apple engineers and designers to"
---

# Get started with the HTML Model Element

---

## Highlight

> 这场 Session 最值得关注的一件事是：Apple 把 visionOS 上的原生 `<model>` 元素下放到了 iOS、iPadOS 和 macOS 的 Safari 中，直接在 Web 标准层面干掉了第三方 3D 库的必要性。

---

## 核心内容

这场 Session 最值得关注的一件事是：Apple 把 visionOS 上的原生 `<model>` 元素下放到了 iOS、iPadOS 和 macOS 的 Safari 中，直接在 Web 标准层面干掉了第三方 3D 库的必要性。

以前在网页上放个 3D 模型，大家默认用 Google 的 `model-viewer` 或者 Three.js，不仅要引入庞大的 JS 库，在 visionOS 上还得专门处理立体渲染（Stereoscopic rendering）的 hack。现在就跟写 `<img>` 标签一样简单，丢个 USDZ 文件进去，浏览器底层直接接管渲染。对于不支持的浏览器，官方提供了 W3C polyfill，甚至还能无缝衔接 AR Quick Look。

这对做电商、3D 资产展示的 Web 开发者来说，直接省掉了一半的前端 3D 胶水代码。你不再需要关心 WebGL 上下文丢失或者内存泄漏，WebKit 底层帮你把脏活累活全干了。


### 最佳实践

已有项目如果 `model-viewer` 跑得好好的，没必要立刻全量替换。新项目或者需要重点优化 visionOS 体验、追求极致首屏性能的，直接上 `<model>`。记得在 HTML 里引入 W3C 的 polyfill 脚本，通过 `window.HTMLModelElement` 判断环境，保证非 Safari 浏览器的基本可用性。

把 `usdcrush` 加到 CI/CD 流程里。USDZ 文件动辄几 MB，压缩后体积能减 75% 且肉眼看不出画质损失，这对移动端首屏加载至关重要。同时用 macOS 自带的 `usdrecord` 命令行工具，写个脚本批量渲染 USDZ 的 fallback 图片，直接替代人工截图。

做自定义旋转动画时，千万记得在 `requestAnimationFrame` 里先 `cancelAnimationFrame` 取消上一个动画。另外，手动设置 `entityTransform` 后，如果发现模型被裁切，需要手动调整 Z 轴位移把模型拉回可视区域，原生元素在自定义模式下不会帮你做防裁剪保护。

---

## 相关 Session

- [Build live production tools for Apple Immersive Video](/articles?year=2026&topic=all&search=338) — go behind the scenes of live apple immersive video production. discover how to p
- [Discover USDKit and what’s new in OpenUSD](/articles?year=2026&topic=all&search=285) — dive into the latest advances in universal scene description (usd) support on ap
- [Explore immersive website environments in visionOS](/articles?year=2026&topic=all&search=320) — transport your website's visitors into virtual environments in apple vision pro 
- [visionOS Group Lab](/articles?year=2026&topic=all&search=8004) — join us online for a deep dive into wwdc26 with apple engineers and designers to
