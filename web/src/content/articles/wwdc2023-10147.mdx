---
title: "Meet Core Location Monitor"
description: "discover how core location monitor can help you better understand location and beacon events in your app. learn how to use core location conditions to describe and track the state of events in your ap"
date: 2026-06-10
tags: ["WWDC 2023"]
thumbnail: "/images/sessions/2023/10147.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2023/10147/"
sessionId: "wwdc2023-10147"
year: "2023"
relatedSessions:
  - title: "Animate symbols in your app"
    code: "10258"
    description: "bring delight to your app with animated symbols. explore the new symbols framewo"
  - title: "Build better document-based apps"
    code: "10056"
    description: "discover how you can use the latest features in ipados to improve your document-"
  - title: "Create a more responsive camera experience"
    code: "10105"
    description: "discover how avcapture and photokit can help you create more responsive and deli"
  - title: "Create animated symbols"
    code: "10257"
    description: "discover animation presets and learn how to use them with sf symbols and custom "
---

# Meet Core Location Monitor

---

## Highlight

> Core Location Frameworks 团队的 Nivash 介绍了全新的 CLMonitor API。这是一个基于 Swift Actor 的位置监听框架，核心用法极其简洁：创建 Monitor、添加条件、await 事件。

---

## 核心内容

Core Location Frameworks 团队的 Nivash 介绍了全新的 CLMonitor API。这是一个基于 Swift Actor 的位置监听框架，核心用法极其简洁：创建 Monitor、添加条件、await 事件。

CLMonitor 是顶层 Swift Actor，每个实例是一个监听网关。因为它是 Actor，所以天然具备线程安全特性，不需要开发者手动处理任务同步问题。创建 Monitor 时传入一个字母数字字符串作为名称——如果同名 Monitor 已存在就打开它，否则创建新的。同一名称同一时刻只能有一个实例。

支持两种条件类型：CircularGeographicCondition（圆形地理区域，类似原来的 CLCircularRegion）和 BeaconIdentityCondition（Beacon 身份条件，类似原来的 CLBeaconIdentityConstraint）。每种条件添加后会被 Core Location 持续监控，状态变化通过事件记录通知。

Session 特别详细地讲解了 Beacon 的三级监控策略：只用 UUID 监控所有站点、UUID + Major 监控特定站点、UUID + Major + Minor 监控特定区域的特定位置。


### 最佳实践

- **用有意义的 identifier**：identifier 不仅是关联条件的键，也是后续查询记录和移除条件的依据。"Work"、"Home" 这样的命名比 "condition1" 好得多。
- **合理使用 assuming 参数**：如果你的应用已经有位置上下文信息，用它来设置初始状态可以减少 Core Location 的判断时间。但不要滥用——错误的初始假设不会导致问题，但会增加状态切换的延迟。
- **移除不再需要的条件**：用 `remove(identifier:)` 清理不再需要的监听条件，减少不必要的系统资源消耗。
- **注意 Monitor 的单例限制**：同一名称同一时刻只能有一个打开的 Monitor 实例。不要在多个地方创建同名 Monitor。
- **权限仍然是前提**：CLMonitor 不改变位置权限的请求方式，仍然需要在使用前获取适当的位置权限。

---

## 相关 Session

- [Animate symbols in your app](/articles?year=2023&topic=all&search=10258) — bring delight to your app with animated symbols. explore the new symbols framewo
- [Build better document-based apps](/articles?year=2023&topic=all&search=10056) — discover how you can use the latest features in ipados to improve your document-
- [Create a more responsive camera experience](/articles?year=2023&topic=all&search=10105) — discover how avcapture and photokit can help you create more responsive and deli
- [Create animated symbols](/articles?year=2023&topic=all&search=10257) — discover animation presets and learn how to use them with sf symbols and custom 
