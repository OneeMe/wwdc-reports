---
title: "Verify identity documents on the web"
description: "learn how digital credentials can enhance online identity verification flows. we"
date: 2026-06-10
tags: ["应用服务", "Safari与Web"]
thumbnail: "/images/sessions/2025/232.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2025/232/"
sessionId: "wwdc2025-232"
year: "2025"
relatedSessions:
  - title: "Discover Apple-Hosted Background Assets"
    code: "325"
    description: "building on background assets, this session will introduce the new capability to"
  - title: "Dive deeper into Writing Tools"
    code: "265"
    description: "with writing tools, people can proofread, rewrite, and transform text directly w"
  - title: "Dive into App Store server APIs for In-App Purchase"
    code: "249"
    description: "discover the latest updates for the app store server api, app store server notif"
  - title: "Enhance child safety with PermissionKit"
    code: "293"
    description: "discover how permissionkit helps you enhance communication safety for children i"
---

## Highlight

> 这个 Session 覆盖了两个完整的 API 实现：网站端的 W3C Digital Credentials API 和 iOS 端的 IdentityDocumentServices framework。

---

## 核心内容

这个 Session 覆盖了两个完整的 API 实现：网站端的 W3C Digital Credentials API 和 iOS 端的 IdentityDocumentServices framework。

网站端的流程是：服务器构建 ISO 18013-7 格式的 mdoc 请求（包含需要验证的字段、加密信息和签名），前端通过 `navigator.credentials.get()` 触发系统 UI，用户选择证件来源（Wallet 或第三方 app），授权后加密响应返回给服务器解密验证。整个链路是端到端加密的，浏览器和操作系统都无法读取身份数据。

iOS 端的 Document Provider API 让任何管理身份证件的 app 都能作为验证来源出现在系统选择 UI 中。app 需要注册证件信息、实现一个 UI App Extension 来显示授权界面，并在用户授权后构建加密响应返回给请求方。关键的安全设计是"partial request"——系统先在沙箱中解析请求的一部分用于构建 UI，完整请求只在用户授权后才释放给 app。


### 最佳实践

建议网站开发者先在 development-signed build 上测试完整流程，因为开发环境不需要 Oblivious HTTP Relay 审批。在提交到 App Store 之前，必须完成 Apple 的 capability 申请流程。

优先使用 `navigator.credentials.get()` 的标准错误处理——API 提供了丰富的异常类型，不要用 try-catch 包裹后静默失败。考虑降级方案：如果 API 不可用（比如旧浏览器），回退到传统的表单上传方式。

避免在请求中只验证最少字段。根据业务需要合理选择验证字段，但也要注意隐私——只请求你真正需要的信息，用户在授权界面会看到完整的请求列表。

---

## 相关 Session

- [Discover Apple-Hosted Background Assets](/articles/wwdc2025-325) — building on background assets, this session will introduce the new capability to
- [Dive deeper into Writing Tools](/articles/wwdc2025-265) — with writing tools, people can proofread, rewrite, and transform text directly w
- [Dive into App Store server APIs for In-App Purchase](/articles/wwdc2025-249) — discover the latest updates for the app store server api, app store server notif
- [Enhance child safety with PermissionKit](/articles/wwdc2025-293) — discover how permissionkit helps you enhance communication safety for children i
