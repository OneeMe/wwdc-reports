---
title: "Adapt ad insertion to Low-Latency HLS"
description: "find out how to integrate advertising into your low-latency hls streams through server-side ad insertion. we"
date: 2026-06-10
tags: ["WWDC 2020"]
thumbnail: "/images/sessions/2020/10232.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2020/10232/"
sessionId: "wwdc2020-10232"
year: "2020"
relatedSessions:
  - title: "Author fragmented MPEG-4 content with AVAssetWriter"
    code: "10011"
    description: "transform your audio and video content into fragmented mpeg-4 files for a faster"
  - title: "Build Metal-based Core Image kernels with Xcode"
    code: "10021"
    description: "learn how to integrate and load core image kernels written in the metal shading "
  - title: "Deliver a better HLS audio experience"
    code: "10158"
    description: "discover techniques for streaming high-quality audio to bandwidth-limited networ"
  - title: "Discover Core Image debugging techniques"
    code: "10089"
    description: "find and fix rendering and optimization issues in your core image pipeline with "
---

# Adapt ad insertion to Low-Latency HLS

---

## Highlight

> 这场 Session 讨论了在 Low-Latency HLS 中实现广告插入（Ad Insertion）的特殊挑战。传统的 HLS 广告插入依赖 `#EXT-X-DATERANGE` 和 `#EXT-X-CUE-OUT` 等标签来标记广告时段，播放器会提前准备广告内容。但在 Low-Latency HLS 中，partial segment 和更高的更新频率让广告插入的时序控制变得更加复杂。

---

## 核心内容

这场 Session 讨论了在 Low-Latency HLS 中实现广告插入（Ad Insertion）的特殊挑战。传统的 HLS 广告插入依赖 `#EXT-X-DATERANGE` 和 `#EXT-X-CUE-OUT` 等标签来标记广告时段，播放器会提前准备广告内容。但在 Low-Latency HLS 中，partial segment 和更高的更新频率让广告插入的时序控制变得更加复杂。

演讲者首先回顾了两种主要的广告插入方式：Client-Side Ad Insertion（CSAI，客户端拼装）和 Server-Side Ad Insertion（SSAI，服务端拼装）。在低延迟场景下，SSAI 更有优势——服务端在推送流时就把广告内容编织进主频道，播放器不需要额外的广告请求，避免了延迟。

Session 的核心部分讲解了如何在 Low-Latency HLS 的 partial segment 级别实现精确的广告切换。关键是用 `#EXT-X-GAP` 标签标记广告边界，利用 `#EXT-X-DATERANGE` 的精确时间戳来控制切换时机，以及如何通过 `#EXT-X-PRELOAD-HINT` 让播放器提前加载广告内容。

最后讨论了广告追踪（Ad Tracking）在低延迟场景下的实现，包括如何在 partial segment 级别触发 impression 和 quartile 事件。


### 最佳实践

- 低延迟直播优先使用 Server-Side Ad Insertion，避免客户端广告请求带来的额外延迟
- 广告的 partial segment duration 应与主内容一致，避免播放器 buffer 策略混乱
- 使用 `#EXT-X-DATERANGE` 而非过时的 `#EXT-X-CUE-OUT` 标记广告时段
- 广告创意预先编码为与主流相同的编解码器和分辨率，避免解码器重置
- 广告追踪事件应与 partial segment 时间对齐，确保数据准确性

---

## 相关 Session

- [Author fragmented MPEG-4 content with AVAssetWriter](/articles?year=2020&topic=all&search=10011) — transform your audio and video content into fragmented mpeg-4 files for a faster
- [Build Metal-based Core Image kernels with Xcode](/articles?year=2020&topic=all&search=10021) — learn how to integrate and load core image kernels written in the metal shading 
- [Deliver a better HLS audio experience](/articles?year=2020&topic=all&search=10158) — discover techniques for streaming high-quality audio to bandwidth-limited networ
- [Discover Core Image debugging techniques](/articles?year=2020&topic=all&search=10089) — find and fix rendering and optimization issues in your core image pipeline with 
