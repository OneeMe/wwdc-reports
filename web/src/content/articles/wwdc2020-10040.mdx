---
title: "Data Essentials in SwiftUI"
description: "data is a complex part of any app, but swiftui makes it easy to ensure a smooth, data-driven experience from prototyping to production. discover @state and @binding, two powerful tools that can preser"
date: 2026-06-10
tags: ["WWDC 2020"]
thumbnail: "/images/sessions/2020/10040.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2020/10040/"
sessionId: "wwdc2020-10040"
year: "2020"
relatedSessions:
  - title: "App essentials in SwiftUI"
    code: "10037"
    description: "thanks to the new app protocol, swiftui now supports building entire apps! see h"
  - title: "Build a SwiftUI view in Swift Playgrounds"
    code: "10643"
    description: "easily prototype and play around with swiftui views when you use them with swift"
  - title: "Build document-based apps in SwiftUI"
    code: "10039"
    description: "learn how to build a document-based app entirely in swiftui! we"
  - title: "Control training in Create ML with Swift"
    code: "10156"
    description: "with the create ml framework you have more power than ever to easily develop mod"
---

# Data Essentials in SwiftUI

---

## Highlight

> SwiftUI 的数据流是它最核心的设计理念，也是初学者最容易困惑的地方。Session 从"数据依赖图"的概念出发，系统梳理了所有数据流相关的属性包装器。

---

## 核心内容

SwiftUI 的数据流是它最核心的设计理念，也是初学者最容易困惑的地方。Session 从"数据依赖图"的概念出发，系统梳理了所有数据流相关的属性包装器。

核心逻辑是：View 是结构体（值类型），不能持有引用类型的状态。SwiftUI 提供了不同的属性包装器来解决这个问题。`@State` 用于 View 内部的简单值类型状态；`@Binding` 用于父子视图之间传递可读写的状态引用；`@StateObject` 用于在 View 中创建和拥有 ObservableObject（iOS 14 新增）；`@ObservedObject` 用于引用外部传入的 ObservableObject；`@EnvironmentObject` 用于跨视图层级共享数据。

Session 还介绍了 2020 年新增的 `@StateObject`，解决了 `@ObservedObject` 在 View 重建时可能多次创建对象的问题。


### 最佳实践

- **简单状态用 @State，复杂状态用 @StateObject。** 如果数据只有几个字段（如 toggle 状态、选中索引），@State 足够。如果数据有多个关联属性或需要跨多个视图共享，用 ObservableObject。
- **创建者用 @StateObject，接收者用 @ObservedObject。** 这是 2020 年的新规则，替代了之前"都用 @ObservedObject"的旧做法。
- **@EnvironmentObject 用于真正需要全局共享的数据。** 如用户信息、App 设置。不要滥用——如果一个数据只有两个视图需要，用 @ObservedObject 传递更清晰。
- **永远不要在 ObservableObject 中使用 @State。** @State 只能用于 View 内部。

---

## 相关 Session

- [App essentials in SwiftUI](/articles?year=2020&topic=all&search=10037) — thanks to the new app protocol, swiftui now supports building entire apps! see h
- [Build a SwiftUI view in Swift Playgrounds](/articles?year=2020&topic=all&search=10643) — easily prototype and play around with swiftui views when you use them with swift
- [Build document-based apps in SwiftUI](/articles?year=2020&topic=all&search=10039) — learn how to build a document-based app entirely in swiftui! we
- [Control training in Create ML with Swift](/articles?year=2020&topic=all&search=10156) — with the create ml framework you have more power than ever to easily develop mod
