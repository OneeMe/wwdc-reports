---
title: "Discover new capabilities in the App Intents framework"
description: "level up your app intents adoption with advanced features to make it faster, more flexible, and more relevant. find out how valuerepresentation and relevantentities make your content more discoverable"
date: 2026-06-10
tags: ["App Intents", "EntityCollection", "LongRunningIntent", "ExecutionTargets", "Apple Intelligence"]
thumbnail: "/images/sessions/2026/345.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/345/"
sessionId: "wwdc2026-345"
year: "2026"
relatedSessions:
  - title: "Best practices for integrating visual intelligence in your app"
    code: "297"
    description: "gain insight on how visual intelligence can transform content discovery in your "
  - title: "Build intelligent Siri experiences with App Schemas"
    code: "240"
    description: "bring your app's content and actions to siri with app intents. model your data u"
  - title: "Code-along: Build powerful drag and drop in SwiftUI"
    code: "271"
    description: "follow along as we build a game of solitaire to explore the latest drag-and-drop"
  - title: "Code-along: Make your app available to Siri"
    code: "344"
    description: "dive deep into an xcode project showing how you can make your app available to s"
---

# Discover new capabilities in the App Intents framework

---

## Highlight

> 过去几年我们用 App Intents（应用意图）接入 Siri 和 Shortcuts（快捷指令）时，总觉得它在“过家家”。传几百个 Entity（实体）给 Intent 会卡死，稍微复杂点的网络请求超过 30 秒就被系统强杀，而且系统经常猜错你的 Intent 该在哪个进程跑。

---

## 核心内容

过去几年我们用 App Intents（应用意图）接入 Siri 和 Shortcuts（快捷指令）时，总觉得它在“过家家”。传几百个 Entity（实体）给 Intent 会卡死，稍微复杂点的网络请求超过 30 秒就被系统强杀，而且系统经常猜错你的 Intent 该在哪个进程跑。

这次 Apple 直接把这几个最恶心的痛点连根拔起。`EntityCollection` 让系统不再傻乎乎地在执行前解析所有 Entity 的全量属性，而是只传 ID，处理 1000 张照片的标签任务从卡死变成瞬秒。`LongRunningIntent` 则彻底打破了 30 秒的执行限制，配合 `ExecutionTargets` 让你能精准控制 Intent 到底是在主 App、Extension 还是 Widget 里跑。

对做效率工具、媒体处理或者重度依赖 Apple Intelligence 的 App 来说，这次更新是刚需。你终于可以把那些需要长时间后台处理、涉及大量本地数据的复杂操作，放心大胆地暴露给 Siri 和 Shortcuts 了。


### 最佳实践

老项目迁移时，优先全局搜索 `[YourEntity]` 类型的 `@Parameter`。如果发现这个 Intent 经常处理超过 50 个实体的数组，立刻把类型换成 `EntityCollection<YourEntity>`，这能直接干掉潜在的 ANR（应用无响应）问题。

新项目设计 App Intents 架构时，把 Intent 定义和实现剥离。利用 `ExecutionTargets` 明确界定边界：涉及 Core Data 写入、Keychain 读取的 Intent 强制指定 `.main`；纯网络请求或数据转换的 Intent 扔到 `.appIntentsExtension`。不要依赖系统的默认启发式分配，那在复杂工程里就是个定时炸弹。

对于需要跨设备流转的 Entity，尽早引入 `SyncableEntity`。如果你的本地 ID 是自增整数或 Core Data 的 Object ID，用 `SyncableEntityIdentifier` 把本地 ID 和 CloudKit Record ID（或服务器 UUID）绑在一起，别等 Siri 跨设备找不到数据了再去改底层数据模型。

---

## 相关 Session

- [Best practices for integrating visual intelligence in your app](/articles?year=2026&topic=all&search=297) — gain insight on how visual intelligence can transform content discovery in your 
- [Build intelligent Siri experiences with App Schemas](/articles?year=2026&topic=all&search=240) — bring your app's content and actions to siri with app intents. model your data u
- [Code-along: Build powerful drag and drop in SwiftUI](/articles?year=2026&topic=all&search=271) — follow along as we build a game of solitaire to explore the latest drag-and-drop
- [Code-along: Make your app available to Siri](/articles?year=2026&topic=all&search=344) — dive deep into an xcode project showing how you can make your app available to s
