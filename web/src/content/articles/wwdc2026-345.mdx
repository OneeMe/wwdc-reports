---
title: "Discover new capabilities in the App Intents framework"
description: "level up your app intents adoption with advanced features to make it faster, more flexible, and more relevant. find out how valuerepresentation and relevantentities make your content more discoverable and allow it to travel across apps, entitycollection improves performance, and syncableentity let you scale across devices. explore richer parameter types including union values and long-running intents that handle cancellation gracefully."
date: 2026-06-10
tags: ["App Intents", "EntityCollection", "LongRunningIntent", "ExecutionTargets", "Apple Intelligence"]
thumbnail: "/images/sessions/2026/345.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/345/"
sessionId: "wwdc2026-345"
year: "2026"
relatedSessions:
  - title: "Explore advanced App Intents features for Siri and Apple Intelligence"
    code: "343"
    description: "polish how your app works with siri using advanced app intents apis."
  - title: "Code-along: Make your app available to Siri"
    code: "344"
    description: "dive deep into an xcode project showing how you can make your app available to siri."
  - title: "Build intelligent Siri experiences with App Schemas"
    code: "240"
    description: "bring your app's content and actions to siri with app intents."
  - title: "Get to know App Intents"
    code: "201"
    description: "learn the core fundamentals of app intents."
---

## Highlight

> 传几百个 Entity 给 Intent 会卡死？网络请求超过 30 秒被系统强杀？Intent 被扔到错误的进程里跑？Apple 这次把这三个最恶心的痛点连根拔起，还加了跨 App 实体共享和场景化推荐。

---

## 核心内容

### 1. 这场演讲解决什么问题

App Intents 框架已经支撑了 Siri、Shortcuts、Spotlight 和 Widgets，但实际使用中开发者遇到了几个硬痛点：大量实体传递性能差、Intent 执行时间受限、进程调度不可控、实体跨 App 共享困难。

这场 Session 针对每个痛点都给了具体的 API 解决方案。

### 2. ValueRepresentation — 跨 App 共享结构化实体

以前用 `Transferable` 传文件或图片给别的 App 没问题，但传结构化数据（如地理位置坐标）时，接收方无法解析。`ValueRepresentation` 解决了这个问题。

```swift
struct LandmarkEntity: AppEntity, Transferable {
    var id: Int
    var landmark: Landmark  // 包含 CLLocationCoordinate2D

    static var transferRepresentation: some TransferRepresentation {
        ValueRepresentation(
            exporting: { entity in
                PlaceDescriptor(
                    representations: [.coordinate(entity.landmark.locationCoordinate)],
                    commonName: entity.landmark.name
                )
            }
        )
    }
}

// 如果实体已有 PlaceDescriptor 属性，用 key-path 更简洁：
struct LandmarkEntity: AppEntity, Transferable {
    var id: Int
    @Property var placeDescriptor: PlaceDescriptor

    static var transferRepresentation: some TransferRepresentation {
        ValueRepresentation(exporting: \.placeDescriptor)
    }
}
```

现在你的 Landmark 可以通过 Shortcuts 传给 Maps 直接导航，而不是只传一个名字。

### 3. RelevantEntities — 在正确的时间推荐正确的内容

告诉系统什么场景下你的内容最相关，Siri 会在合适的时机主动推荐。

```swift
// 用户设置跑步训练时，推荐跑步歌单
let playlistEntities = [dailyRun, runningMix]
let workoutContext = AppEntityContext.audio(.workout(activityType: .running))

try await RelevantEntities.shared.updateEntities(playlistEntities, for: workoutContext)

// 清除特定场景下的推荐
try await RelevantEntities.shared.removeAllEntities(for: workoutContext)

// 清除所有推荐
try await RelevantEntities.shared.removeAllEntities()
```

### 4. EntityCollection — 处理大量实体不卡死

以前传 `[PhotoEntity]` 给 Intent，系统会在执行前解析所有实体的全量属性。1000 张照片直接卡死。现在用 `EntityCollection` 只传 ID。

```swift
struct TagPhotosIntent: AppIntent {
    static let title: LocalizedStringResource = "Tag Travel Photos"

    @Parameter var photos: EntityCollection<PhotoEntity>   // 以前是: [PhotoEntity]
    @Parameter var tag: String

    func perform() async throws -> some IntentResult {
        modelData.tagPhotos(ids: photos.identifiers, tag: tag)  // 只传 ID
        return .result()
    }
}
```

### 5. SyncableEntity — 跨设备同步实体 ID

Siri 在 iPhone 上创建了一个提醒，用户在 iPad 上问"我昨天创建的提醒是什么"，系统需要能跨设备匹配同一个实体。

```swift
// ID 本身已经是稳定的（如服务器 UUID、CloudKit record ID）
struct PhotoEntity: AppEntity, SyncableEntity {
    var id: Int  // 已经是跨设备稳定的 —— 就这么简单
}

// 本地 ID 不稳定，需要配对稳定 ID
struct PhotoEntity: AppEntity, SyncableEntity {
    var id: SyncableEntityIdentifier<String, String>

    init(localID: String, stableID: String) {
        self.id = SyncableEntityIdentifier(local: localID, stable: stableID)
    }
}
```

### 6. @UnionValue — 参数接受多种类型

一个参数可能需要接受不同类型的实体，比如"添加到收藏"既可以收藏地标也可以收藏相册。

```swift
@UnionValue
enum TravelGalleryContent {
    case landmarkCollection(LandmarkCollectionEntity)
    case photoAlbum(PhotoAlbumEntity)

    static let typeDisplayRepresentation: TypeDisplayRepresentation = "Travel Gallery"
    static let caseDisplayRepresentations: [Cases: DisplayRepresentation] = [
        .landmarkCollection: "Landmark Collection",
        .photoAlbum: "Photo Album"
    ]
}
```

### 7. LongRunningIntent — 打破 30 秒限制

上传文件、批量处理等耗时操作以前会被系统杀掉。现在用 `LongRunningIntent` + `CancellableIntent`：

```swift
struct UploadPhotoIntent: LongRunningIntent, CancellableIntent {
    static let title: LocalizedStringResource = "Upload Photo"

    @Parameter var photo: IntentFile

    func perform() async throws -> some IntentResult & ProvidesDialog {
        let result = try await performBackgroundTask {
            let chunks = calculateChunks(for: photo)
            progress.totalUnitCount = Int64(chunks)

            for chunk in 1...chunks {
                try Task.checkCancellation()
                try await uploadChunk(chunk)
                progress.completedUnitCount = Int64(chunk)
            }
            return "Upload complete!"
        } onCancel: { reason in
            cleanup(for: reason)
        }
        return .result(dialog: "\(result)")
    }
}
```

### 8. ExecutionTargets — 控制 Intent 在哪个进程运行

```swift
// 写操作 —— 需要主 App
struct UpdateFavoriteIntent: AppIntent {
    static var allowedExecutionTargets: ExecutionTargets { .main }
}

// 独立下载 —— 在 Extension 里跑
struct DownloadPhotoIntent: AppIntent {
    static var allowedExecutionTargets: ExecutionTargets { .appIntentsExtension }
}

// 显示类 —— 在 Widget Extension 里跑
struct GetLandmarkStatusIntent: AppIntent {
    static var allowedExecutionTargets: ExecutionTargets { .widgetKitExtension }
}

// 都行 —— 让系统选
struct TagPhotosIntent: AppIntent {
    static var allowedExecutionTargets: ExecutionTargets { [.main, .appIntentsExtension] }
}
```

---

## 最佳实践

- **已有项目迁移**：全局搜索 `[YourEntity]` 类型的 `@Parameter`。如果 Intent 经常处理超过 50 个实体的数组，立刻换成 `EntityCollection<YourEntity>`，这能直接干掉潜在的 ANR 问题。
- **新项目设计**：把 Intent 定义和实现剥离。利用 `ExecutionTargets` 明确界定边界：涉及 Core Data 写入、Keychain 读取的 Intent 强制指定 `.main`；纯网络请求或数据转换的 Intent 扔到 `.appIntentsExtension`。
- **容易踩的坑**：`SyncableEntity` 的 stable ID 必须在设备间真正稳定。不要用自增整数或本地文件路径，至少用 UUID 或服务器返回的 ID。

---

## 相关 Session

- [Explore advanced App Intents features for Siri and Apple Intelligence](/articles/wwdc2026-343) — 自定义对话、屏幕感知进阶
- [Code-along: Make your app available to Siri](/articles/wwdc2026-344) — 手把手接入 Siri
- [Build intelligent Siri experiences with App Schemas](/articles/wwdc2026-240) — App Schemas 概念详解
- [Get to know App Intents](/articles/wwdc2026-201) — App Intents 基础入门
