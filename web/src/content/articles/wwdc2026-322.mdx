---
title: "Compose advanced graphics effects with SwiftUI"
description: "discover how to craft rich, custom experiences by creatively composing swiftui layout and graphics apis. we'll show you how to break down complex designs and use a creative pipeline to chain simple bu"
date: 2026-06-10
tags: ["SwiftUI", "Metal Shader", "layerEffect", "alignmentGuide", "TimelineView"]
thumbnail: "/images/sessions/2026/322.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/322/"
sessionId: "wwdc2026-322"
year: "2026"
relatedSessions:
  - title: "Communicate your brand identity on iOS"
    code: "251"
    description: "explore practical approaches to branding using typography, color, components, an"
  - title: "Craft clear names for features and labels in your app"
    code: "290"
    description: "the names of your products, features, and labels can determine how people unders"
  - title: "Dive into lazy stacks and scrolling with SwiftUI"
    code: "321"
    description: "discover the inner workings of lazy stacks in swiftui. we'll explore how lazyvst"
  - title: "Learn CSS Grid Lanes"
    code: "314"
    description: "build adaptive web layouts that embrace content of all shapes and sizes. explore"
---

# Compose advanced graphics effects with SwiftUI

---

## Highlight

> 以前我们要在 SwiftUI 里做类似 Apple Music 实时歌词那种带背景流体扭曲、歌词高亮且边缘带浮动时间戳的界面，大概率要捏着鼻子去写 `UIViewRepresentable` 或者手算 `offset` 坐标。这次 Apple 的工程师 Haotian 直接拿一个播客 App 开刀，演示了怎么把复杂设计拆解成“创意管道 (Creative Pipeline)”。

---

## 核心内容

以前我们要在 SwiftUI 里做类似 Apple Music 实时歌词那种带背景流体扭曲、歌词高亮且边缘带浮动时间戳的界面，大概率要捏着鼻子去写 `UIViewRepresentable` 或者手算 `offset` 坐标。这次 Apple 的工程师 Haotian 直接拿一个播客 App 开刀，演示了怎么把复杂设计拆解成“创意管道 (Creative Pipeline)”。

他把数据流分成了几个管道：用 `layerEffect` 配合 Metal Shader (着色器) 和 NoiseTexture (噪点纹理) 做背景流体扭曲，用 `TimelineView` (时间线视图) 给无状态的 Shader 注入时间参数驱动动画，最绝的是用 `alignmentGuide` 让时间戳浮动在歌词行底部外侧而不破坏原有布局。这对那些受够了 SwiftUI 默认布局限制、想做高定 UI 的开发者来说，简直是打通了任督二脉。


### 最佳实践

已有项目如果充斥着为了做复杂动效而引入的 `UIViewRepresentable` 和 Core Animation 胶水代码，现在可以逐步用 `TimelineView` + `layerEffect` 替换掉。特别是那些依赖 `CADisplayLink` 驱动的重绘逻辑，换成 `TimelineView(.animation)` 后代码量能砍掉一半。

新项目在构思复杂 UI 时，强迫自己用“管道 (Pipeline)”思维。不要试图在一个巨大的 `body` 里写死所有状态，把数据源（如音频时间戳）、变换层（如 Shader）和展示层（如 Text）拆开。让上一级的输出直接作为下一级的输入，比如把播放时间同时喂给 Shader 做水波纹和喂给 `ScrollView` 做歌词滚动。

实战中最容易踩的坑是忘了 Shader 是无状态的。很多开发者习惯在 SwiftUI 里用 `@State` 和 `withAnimation` 做动画，但 Metal Shader 根本不吃这一套。Shader 每一帧都是独立计算的，你必须用 `TimelineView` 获取当前时间戳，显式地把它作为参数传给 Shader，它才能动起来。

---

## 相关 Session

- [Communicate your brand identity on iOS](/articles?year=2026&topic=all&search=251) — explore practical approaches to branding using typography, color, components, an
- [Craft clear names for features and labels in your app](/articles?year=2026&topic=all&search=290) — the names of your products, features, and labels can determine how people unders
- [Dive into lazy stacks and scrolling with SwiftUI](/articles?year=2026&topic=all&search=321) — discover the inner workings of lazy stacks in swiftui. we'll explore how lazyvst
- [Learn CSS Grid Lanes](/articles?year=2026&topic=all&search=314) — build adaptive web layouts that embrace content of all shapes and sizes. explore
