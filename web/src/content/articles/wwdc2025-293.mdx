---
title: "Enhance child safety with PermissionKit"
description: "discover how permissionkit helps you enhance communication safety for children in your app. we"
date: 2026-06-10
tags: ["应用服务", "隐私与安全"]
thumbnail: "/images/sessions/2025/293.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2025/293/"
sessionId: "wwdc2025-293"
year: "2025"
relatedSessions:
  - title: "Discover Apple-Hosted Background Assets"
    code: "325"
    description: "building on background assets, this session will introduce the new capability to"
  - title: "Dive deeper into Writing Tools"
    code: "265"
    description: "with writing tools, people can proofread, rewrite, and transform text directly w"
  - title: "Dive into App Store server APIs for In-App Purchase"
    code: "249"
    description: "discover the latest updates for the app store server api, app store server notif"
  - title: "Explore enhancements to your spatial business app"
    code: "223"
    description: "discover how the latest enhancements and apis in visionos 26 expand access and e"
---

# Enhance child safety with PermissionKit

---

## Highlight

> PermissionKit 解决的问题很具体：当一个孩子在 app 中想和一个陌生人开始通信时，app 如何安全地获取父母的许可？

---

## 核心内容

PermissionKit 解决的问题很具体：当一个孩子在 app 中想和一个陌生人开始通信时，app 如何安全地获取父母的许可？

**核心流程：**
1. App 检测到当前用户是孩子，且对方是未知联系人。
2. App 创建一个 `PermissionQuestion`，附带 `CommunicationTopic`（包含对方的 handle、姓名、头像、通信方式如 message/call/video）。
3. 系统在 Messages 中打开一个预填好的消息窗口，收件人是 Family Sharing 组中的父母/监护人。
4. 父母收到消息后可以直接 decline 或 review（review 会显示更多上下文信息），做出 approve 或 decline 决定。
5. 父母的决定自动通知孩子，同时在后台送达你的 app。

**判断用户是否为孩子的两种方式：** 用你自己的账号系统，或者用新的 Declared Age Range API（父母可以允许孩子分享年龄范围）。

**UI 适配：** 对孩子的账户，默认隐藏未知联系人的消息预览、头像等内容。用 `CommunicationLimits.knownHandles(in:)` 做批量查找，返回已知 handle 的子集。

**SwiftUI 集成：** `CommunicationLimitsButton` 直接在 SwiftUI 视图中使用，点击触发系统流程。UIKit 用 `CommunicationLimits` singleton 配合 UIViewController。AppKit 配合 NSWindow。

**异步响应处理：** 通过 `CommunicationLimits` singleton 的 AsyncSequence 持续监听父母的审批结果。App 会以 background launch 方式唤醒。


### 最佳实践

**只对确认是孩子的用户展示 PermissionKit 流程。** 误用（对成人用户展示）会让用户体验变差且可能违反 App Store 审核规则。Declared Age Range API 是判断年龄的推荐方式。

**提供丰富的 metadata。** 对方的姓名和头像会显著提升父母的审批意愿和决策质量。但只收集你合法拥有的信息。

**不要依赖 PermissionKit 做唯一的安全机制。** 它是 Apple 生态内的解决方案。跨平台场景（你的 web 版、Android 版）需要你自己实现类似逻辑。把 PermissionKit 的审批结果同步到你自己的服务器，作为统一的权限来源。

**处理好"没有 Family Sharing"的情况。** API 会返回默认响应，但你应该在 UI 上告诉用户发生了什么，而不是静默失败。

---

## 相关 Session

- [Discover Apple-Hosted Background Assets](/articles?year=2025&topic=all&search=325) — building on background assets, this session will introduce the new capability to
- [Dive deeper into Writing Tools](/articles?year=2025&topic=all&search=265) — with writing tools, people can proofread, rewrite, and transform text directly w
- [Dive into App Store server APIs for In-App Purchase](/articles?year=2025&topic=all&search=249) — discover the latest updates for the app store server api, app store server notif
- [Explore enhancements to your spatial business app](/articles?year=2025&topic=all&search=223) — discover how the latest enhancements and apis in visionos 26 expand access and e
