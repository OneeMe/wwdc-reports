---
title: "Build dynamic iOS apps with the Create ML framework"
description: "discover how your app can train core ml models fully on device with the create ml framework, enabling adaptive and customized app experiences, all while preserving data privacy. we"
date: 2026-06-10
tags: ["WWDC 2021"]
thumbnail: "/images/sessions/2021/10037.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2021/10037/"
sessionId: "wwdc2021-10037"
year: "2021"
relatedSessions:
  - title: "Capture high-quality photos using video formats"
    code: "10247"
    description: "your app can take full advantage of the powerful camera systems on iphone by usi"
  - title: "Explore low-latency video encoding with VideoToolbox"
    code: "10158"
    description: "supporting low latency encoders has become an important aspect of video applicat"
  - title: "Improve access to Photos in your app"
    code: "10046"
    description: "phpicker is the simplest and most secure way to integrate the photos library int"
  - title: "Use the camera for keyboard input in your app"
    code: "10276"
    description: "learn how you can support live text and intelligently pull information from the "
---

## Highlight

> Create ML 之前只能在 Mac 上训练模型，然后把训练好的 `.mlmodel` 文件打包到 app 中。iOS 15 打破了这个限制：`CreateMLComponents` 框架允许你在 iOS 设备上直接训练和推理机器学习模型。

---

## 核心内容

Create ML 之前只能在 Mac 上训练模型，然后把训练好的 `.mlmodel` 文件打包到 app 中。iOS 15 打破了这个限制：`CreateMLComponents` 框架允许你在 iOS 设备上直接训练和推理机器学习模型。

Session 展示了几个使用场景。照片分类 app 可以根据用户的相册训练一个专属的分类器。健身 app 可以根据用户的动作数据优化姿态检测模型。文本输入 app 可以学习用户的打字习惯来改进自动补全。

核心 API 是 `MLPipeline`，它把数据预处理、特征提取和模型训练串成一条流水线。配合 `MLUpdateTask`，你可以在后台执行训练任务，不会阻塞 UI。训练完成后，模型直接保存在设备的 Application Support 目录中，下次启动 app 时加载即可。


### 最佳实践

设备端训练不应该在用户首次启动 app 时就执行。应该在用户产生了足够的数据（比如使用了几天）后再提示「是否启用个性化功能」。训练过程要在后台进行，并且允许用户取消。

模型文件的大小需要关注。一个 fine-tune 后的图片分类模型可能在 10-50MB。如果你的 app 有多个模型，考虑用 `MLModelCollection` 来管理，系统会在存储空间不足时自动清理不常用的模型。

---

## 相关 Session

- [Capture high-quality photos using video formats](/articles/wwdc2021-10247) — your app can take full advantage of the powerful camera systems on iphone by usi
- [Explore low-latency video encoding with VideoToolbox](/articles/wwdc2021-10158) — supporting low latency encoders has become an important aspect of video applicat
- [Improve access to Photos in your app](/articles/wwdc2021-10046) — phpicker is the simplest and most secure way to integrate the photos library int
- [Use the camera for keyboard input in your app](/articles/wwdc2021-10276) — learn how you can support live text and intelligently pull information from the 
