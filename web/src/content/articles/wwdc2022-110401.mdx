---
title: "Create Swift Package plugins"
description: "tailor your development workflow and learn how to write your own package plugins in swift. we"
date: 2026-06-10
tags: ["WWDC 2022"]
thumbnail: "/images/sessions/2022/110401.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2022/110401/"
sessionId: "wwdc2022-110401"
year: "2022"
relatedSessions:
  - title: "Debug Swift debugging with LLDB"
    code: "110370"
    description: "learn how you can set up complex swift projects for debugging. we"
  - title: "Deep dive into Xcode Cloud for teams"
    code: "110375"
    description: "learn how you can use apple"
  - title: "Demystify parallelization in Xcode builds"
    code: "110364"
    description: "learn how the xcode build system extracts maximum parallelism from your builds. "
  - title: "Get the most out of Xcode Cloud"
    code: "110374"
    description: "discover how you can get the most out of xcode cloud, apple"
---

# Create Swift Package plugins

---

## Highlight

> Boris 主讲的这场 Session 系统性地介绍了 Swift Package Plugins 的创建方式。从基本的自定义命令插件（如从 Git 历史生成 CONTRIBUTORS 文件），到构建工具插件（分为 in-build 和 pre-build 两种类型），再到权限声明和沙盒机制。

---

## 核心内容

Boris 主讲的这场 Session 系统性地介绍了 Swift Package Plugins 的创建方式。从基本的自定义命令插件（如从 Git 历史生成 CONTRIBUTORS 文件），到构建工具插件（分为 in-build 和 pre-build 两种类型），再到权限声明和沙盒机制。

核心概念：插件是用 Swift 编写的代码，使用 PackagePlugin API（类似 Package Manifest）。插件运行在沙盒中，网络访问受限，默认只能写临时目录。自定义命令可以选择声明写入 Package 根目录的权限。构建工具插件通过声明输入/输出关系，让构建系统自动判断是否需要重新执行。


### 最佳实践

- **Tools version 最低 5.6。** 插件功能从这个版本开始支持。
- **谨慎使用 pre-build command。** 每次构建都会执行，如果没有缓存机制，会严重拖慢构建速度。
- **为权限声明写清晰的 reason。** 用户看到这段文字来决定是否信任你的插件，模糊的描述会导致用户拒绝授权。
- **利用 `try process.run()` 执行外部工具。** 插件可以通过 Process API 调用 Git、SwiftFormat 等命令行工具，但要注意工具必须在沙盒限制下正常工作。

---

## 相关 Session

- [Debug Swift debugging with LLDB](/articles?year=2022&topic=all&search=110370) — learn how you can set up complex swift projects for debugging. we
- [Deep dive into Xcode Cloud for teams](/articles?year=2022&topic=all&search=110375) — learn how you can use apple
- [Demystify parallelization in Xcode builds](/articles?year=2022&topic=all&search=110364) — learn how the xcode build system extracts maximum parallelism from your builds. 
- [Get the most out of Xcode Cloud](/articles?year=2022&topic=all&search=110374) — discover how you can get the most out of xcode cloud, apple
