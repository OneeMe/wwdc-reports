---
title: "Live Activities essentials"
description: "elevate your app experience with live activities. explore many of the places where live activities appear, including a new style in the dynamic island that delivers more information when iphone is used in landscape. learn how to tailor your live activity for each space, structure your content and data, and drive real time updates from start to finish using activitykit and push notifications."
date: 2026-06-10
tags: ["Live Activities", "ActivityKit", "Dynamic Island", "WidgetKit", "SwiftUI"]
thumbnail: "/images/sessions/2026/223.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/223/"
sessionId: "wwdc2026-223"
year: "2026"
relatedSessions:
  - title: "Design dynamic Live Activities"
    code: "209"
    description: "discover how to design live activities that look great and provide the right inf"
  - title: "Create live communication experiences"
    code: "226"
    description: "livecommunicationkit transforms your real-time communication apps into integrate"
  - title: "Enhance the accessibility of your reading app"
    code: "219"
    description: "learn how to create robust reading experiences for voiceover, speak screen, and "
---

## Highlight

> iOS 27 把 Live Activities 的展示面从竖屏锁屏和灵动岛扩展到了横屏灵动岛、StandBy、Apple Watch Smart Stack 甚至 CarPlay Dashboard。新增的 `isDynamicIslandLimitedInWidth` 和 `activityFamily` 环境变量让你能精准裁剪内容，而 `LiveActivityIntent` 则让用户可以直接在实时活动上完成打分、确认等轻量交互，不用跳回 App。

---

## 核心内容

### 1. 这场演讲解决什么问题

以前做 Live Activities，开发者基本只盯着竖屏锁屏和灵动岛：

- 横屏打游戏或看视频时，灵动岛里的实时活动直接装死
- 多端尺寸差异导致 UI 崩溃或信息截断
- 实时活动只能"看"，不能"操作"

iOS 27 解决了这三个问题：横屏支持、多尺寸适配、交互能力。

### 2. 数据模型：分离静态和动态数据

Live Activities 把数据分成两部分，这是为了高效更新：

- **ActivityAttributes**：静态数据，创建后不可变（如店铺名、订单号）
- **ContentState**：动态数据，可以实时更新（如订单状态、剩余时间）

```swift
import ActivityKit

public struct DrinkOrderAttributes: ActivityAttributes {
    let shopName: String
    let drink: Drink
    let orderID: UUID

    public struct ContentState: Codable, Hashable {
        var phase: DrinkOrder.Phase = .waiting
        var estimatedReadyDate: Date
        var rating: DrinkOrder.Rating?
    }
}
```

### 3. 视图配置：覆盖所有展示场景

用 WidgetKit 的 `ActivityConfiguration` 定义所有展示形态：

```swift
struct DrinkOrderLiveActivity: Widget {
    var body: some WidgetConfiguration {
        ActivityConfiguration(for: DrinkOrderAttributes.self) { context in
            // 锁屏、StandBy 等场景
            ActivityView(context: context)
        } dynamicIsland: { context in
            DynamicIsland {
                // 展开形态
                DynamicIslandExpandedRegion(.leading) {
                    ExpandedLeadingView(context: context)
                }
                DynamicIslandExpandedRegion(.center) {
                    ExpandedCenterView(context: context)
                }
                DynamicIslandExpandedRegion(.trailing) {
                    ExpandedTrailingView(context: context)
                }
                DynamicIslandExpandedRegion(.bottom) {
                    ExpandedBottomView(context: context)
                }
            } compactLeading: {
                CompactLeadingView(context: context)
            } compactTrailing: {
                CompactTrailingView(context: context)
            } minimal: {
                MinimalView(context: context)
            }
        }
    }
}
```

### 4. 启动和更新

```swift
// 启动 Live Activity
func launchLiveActivity(order: DrinkOrder) throws {
    guard ActivityAuthorizationInfo().areActivitiesEnabled else { return }
    let attributes = DrinkOrderAttributes(
        shopName: "Coffee Shop",
        drink: order.drink,
        orderID: order.id
    )
    let estimatedReadyDate = Date.now + (15 * 60)
    let contentState = DrinkOrderAttributes.ContentState(
        phase: .waiting,
        estimatedReadyDate: estimatedReadyDate
    )
    let activityContent = ActivityContent(state: contentState, staleDate: nil)
    let activity = try Activity.request(
        attributes: attributes,
        content: activityContent
    )
}

// 更新 Live Activity
await activity.update(
    ActivityContent(
        state: DrinkOrderAttributes.ContentState(
            phase: .preparing,
            estimatedReadyDate: estimatedReadyDate
        ),
        staleDate: nil
    )
)
```

### 5. 横屏和窄宽度适配

iOS 27 新增了 `isDynamicIslandLimitedInWidth` 环境变量，横屏时灵动岛会变窄：

```swift
struct CompactTrailingView: View {
    @Environment(\.isDynamicIslandLimitedInWidth) var isDynamicIslandLimitedInWidth
    var context: ActivityViewContext<DrinkOrderAttributes>

    var body: some View {
        if isDynamicIslandLimitedInWidth {
            // 横屏窄宽度：只显示图标
            StepProgressIconView(context: context)
        } else if context.state.phase.showsTimer {
            // 竖屏正常宽度：显示倒计时
            EstimatedReadyView(context: context)
        } else {
            // 显示状态标签
            OrderPhaseLabelView(context: context)
        }
    }
}
```

### 6. StandBy 背景色扩展

在 StandBy 模式下，可以用 `.activityBackgroundTint` 设置背景色：

```swift
struct ActivityView: View {
    @Environment(\.showsWidgetContainerBackground) var showsWidgetContainerBackground
    var context: ActivityViewContext<DrinkOrderAttributes>

    var body: some View {
        DetailView(context: context)
            .background {
                if showsWidgetContainerBackground {
                    LinearGradient.barista
                }
            }
            .activityBackgroundTint(.espresso)
    }
}
```

### 7. Small Family 支持

iOS 27 新增了 `.small` 的 activity family，适用于 Apple Watch 等空间有限的场景：

```swift
struct DrinkOrderLiveActivity: Widget {
    var body: some WidgetConfiguration {
        ActivityConfiguration(for: DrinkOrderAttributes.self) { context in
            ActivityView(context: context)
        } dynamicIsland: { context in
            // ... Dynamic Island 配置
        }
        .supplementalActivityFamilies([.small])
    }
}

struct ActivityView: View {
    @Environment(\.activityFamily) var activityFamily

    var body: some View {
        if activityFamily == .small {
            SmallView(context: context)
        } else {
            DetailView(context: context)
        }
    }
}
```

### 8. 交互：LiveActivityIntent

让用户直接在实时活动上操作，不用打开 App：

```swift
struct RateDrinkIntent: LiveActivityIntent {
    static var title: LocalizedStringResource = "Rate Drink"

    @Parameter(title: "Order ID")
    var orderID: String

    @Parameter(title: "Positive")
    var isPositive: Bool

    func perform() async throws -> some IntentResult {
        await updateLocalDatastore(
            rating: isPositive ? .great : .poor,
            dismissPolicy: .after(.now + 15)
        )
        return .result()
    }
}
```

```swift
struct RatingButtons: View {
    var context: ActivityViewContext<DrinkOrderAttributes>
    var body: some View {
        HStack(spacing: 12) {
            Button(intent: RateDrinkIntent(
                orderID: context.attributes.orderID.uuidString,
                isPositive: false
            )) {
                Label("Not Good", systemImage: "hand.thumbsdown.fill")
            }
            .buttonStyle(RatingButtonStyle(color: .red))

            Button(intent: RateDrinkIntent(
                orderID: context.attributes.orderID.uuidString,
                isPositive: true
            )) {
                Label("Great", systemImage: "hand.thumbsup.fill")
            }
            .buttonStyle(RatingButtonStyle(color: .green))
        }
    }
}
```

---

## 最佳实践

- **已有项目迁移**：如果你之前的实时活动只写了锁屏和灵动岛竖屏视图，现在必须加上 `isDynamicIslandLimitedInWidth` 和 `activityFamily` 的判断。不用重写整个 UI，只需要在现有的 SwiftUI 视图外层套一层条件判断，把长文本换成图标，把复杂列表换成单行状态即可
- **新项目建议**：严格分离静态数据和动态数据。把永远不变的字段（如店铺名、订单号）放在 `ActivityAttributes` 里，把随时间变化的字段（如状态、倒计时）放在 `ContentState` 里。这不仅是 Apple 的强制要求，更能大幅减少推送更新的 payload 体积
- **推送策略**：如果你的 App 是赛事直播，几万人同时看一场比赛，一定要用广播频道（Channel）推送更新，而不是给每个设备单独发 Push。单播推送在并发量高时极易触发 APNs 的限流，导致部分用户的实时活动卡死在旧状态

---

## 相关 Session

- [Design dynamic Live Activities](/articles/wwdc2026-209) — Live Activities 的设计指南
- [Create live communication experiences](/articles/wwdc2026-226) — 实时通信体验与 Live Activities 的结合
- [Enhance the accessibility of your reading app](/articles/wwdc2026-219) — 辅助功能最佳实践
