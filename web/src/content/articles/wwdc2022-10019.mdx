---
title: "Get to know Create ML Components"
description: "create ml makes it easy to build custom machine learning models for image classification, object detection, sound classification, hand pose classification, action classification, tabular data regressi"
date: 2026-06-10
tags: ["WWDC 2022"]
thumbnail: "/images/sessions/2022/10019.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2022/10019/"
sessionId: "wwdc2022-10019"
year: "2022"
relatedSessions:
  - title: "Compose advanced models with Create ML Components"
    code: "10020"
    description: "take your custom machine learning models to the next level with create ml compon"
  - title: "Explore the machine learning development experience"
    code: "10017"
    description: "learn how to bring great machine learning (ml) based experiences to your app. we"
  - title: "Meet the Presenter: Accelerate machine learning with Metal"
    code: "110906"
    description: "meet the presenters of “accelerate machine learning with metal” and join a text-"
  - title: "Meet the Presenter: Compose advanced models with Create ML Components"
    code: "110904"
    description: "meet the presenters of “compose advanced models with create ml components” and j"
---

## Highlight

> Create ML 提供了一系列预定义的 ML 任务：图像分类、声音分类、动作分类等。但如果你想做的任务不在列表里怎么办？比如给香蕉的成熟度打分（图像回归），或者预测牛油果的价格（表格回归）？

---

## 核心内容

Create ML 提供了一系列预定义的 ML 任务：图像分类、声音分类、动作分类等。但如果你想做的任务不在列表里怎么办？比如给香蕉的成熟度打分（图像回归），或者预测牛油果的价格（表格回归）？

Create ML Components 就是为此而生的。它把 ML 任务拆成了两种基本单元：Transformer（对数据做变换）和 Estimator（从数据中学习并产生 Transformer）。你用 `appending` 方法把组件串起来，就像搭积木一样构建自定义任务。

Session 用两个完整 demo 展示了这个框架的威力。第一个是图像回归器：给香蕉照片的成熟度打 1-10 分。Feature extractor + Linear Regressor，几行代码就搞定。然后通过数据增强（随机旋转和缩放）和 Vision 框架的显著性裁剪来提升模型精度。第二个是表格回归器：预测牛油果价格。用 ColumnSelector 选出需要标准化的数值列，用 StandardScaler 做归一化，再接上 BoostedTreeRegressor。


### 最佳实践

- 用 `AnnotatedFiles` 的 `labeledByNamesAt` 从文件名或目录结构自动提取标注。
- 用 `randomSplit(by:)` 划分训练集和验证集，0.8 是个不错的起点。
- 在 `fitted` 方法中传入 event handler 监控训练过程中的验证指标。
- 数据不够时，用 `flatMap` 做数据增强（旋转、缩放），注意只在训练集上增强。
- 部署方式二选一：导出 Core ML 模型（不支持自定义组件），或者打包 Swift package（支持所有组件）。

---

## 相关 Session

- [Compose advanced models with Create ML Components](/articles/wwdc2022-10020) — take your custom machine learning models to the next level with create ml compon
- [Explore the machine learning development experience](/articles/wwdc2022-10017) — learn how to bring great machine learning (ml) based experiences to your app. we
- [Meet the Presenter: Accelerate machine learning with Metal](/articles/wwdc2022-110906) — meet the presenters of “accelerate machine learning with metal” and join a text-
- [Meet the Presenter: Compose advanced models with Create ML Components](/articles/wwdc2022-110904) — meet the presenters of “compose advanced models with create ml components” and j
