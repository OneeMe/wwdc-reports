---
title: "Detect and diagnose memory issues"
description: "discover how you can understand and diagnose memory performance problems with xcode. we"
date: 2026-06-10
tags: ["WWDC 2021"]
thumbnail: "/images/sessions/2021/10180.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2021/10180/"
sessionId: "wwdc2021-10180"
year: "2021"
relatedSessions:
  - title: "Build interactive tutorials using DocC"
    code: "10235"
    description: "discover how you can author immersive tutorials from scratch with docc. we"
  - title: "Customize your advanced Xcode Cloud workflows"
    code: "10269"
    description: "xcode cloud integrates with apple developer tools and services, all major source"
  - title: "Detect bugs early with the static analyzer"
    code: "10202"
    description: "discover how xcode can automatically track down infinite loops, unused code, and"
  - title: "Diagnose unreliable code with test repetitions"
    code: "10296"
    description: "test repetitions can help you debug even the most unreliable code. discover how "
---

## Highlight

> Session 系统性地讲解了如何在 iOS 和 macOS 上检测、诊断和修复内存问题。覆盖了三类常见的内存问题：内存泄漏（Memory Leak，已分配的内存无法被释放）、废弃内存（Abandoned Memory，仍然被引用但不再需要的数据）、和过度分配（Over-allocation，一次性分配了远超需要的大块内存）。

---

## 核心内容

Session 系统性地讲解了如何在 iOS 和 macOS 上检测、诊断和修复内存问题。覆盖了三类常见的内存问题：内存泄漏（Memory Leak，已分配的内存无法被释放）、废弃内存（Abandoned Memory，仍然被引用但不再需要的数据）、和过度分配（Over-allocation，一次性分配了远超需要的大块内存）。

工具方面，Session 重点介绍了 Xcode 13 中改进的 Memory Graph Debugger、Instruments 的 Allocations 和 Leaks 模板、以及新的 `os_proc_available_memory()` API。Memory Graph Debugger 现在可以导出内存图快照（memgraph 文件），然后用命令行工具 `leaks`、`heap` 和 `vmmap` 做深度分析。这种"GUI 抓快照 + CLI 做分析"的工作流比在 Xcode GUI 里翻来翻去高效得多。


### 最佳实践

- 在 App 开发过程中每周做一次 memgraph 快照分析：在典型使用场景下（浏览几个页面、创建一些内容）抓一个快照，用 `heap --sortBySize` 检查是否有异常大的内存占用。
- `os_proc_available_memory()` 在加载大资源（图片、视频、3D 模型）前调用。如果可用内存不足，先清理缓存再加载——比加载失败后再处理用户体验更好。
- Leaks 和 Abandoned Memory 要分开检测：Leaks 用 Instruments 的 Leaks 模板自动检测，Abandoned Memory 用 "Mark Heap" 功能对比两个时间点的内存图差异。

---

## 相关 Session

- [Build interactive tutorials using DocC](/articles/wwdc2021-10235) — discover how you can author immersive tutorials from scratch with docc. we
- [Customize your advanced Xcode Cloud workflows](/articles/wwdc2021-10269) — xcode cloud integrates with apple developer tools and services, all major source
- [Detect bugs early with the static analyzer](/articles/wwdc2021-10202) — discover how xcode can automatically track down infinite loops, unused code, and
- [Diagnose unreliable code with test repetitions](/articles/wwdc2021-10296) — test repetitions can help you debug even the most unreliable code. discover how 
