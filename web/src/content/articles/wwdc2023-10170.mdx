---
title: "Beyond the basics of structured concurrency"
description: "it"
date: 2026-06-10
tags: ["WWDC 2023"]
thumbnail: "/images/sessions/2023/10170.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2023/10170/"
sessionId: "wwdc2023-10170"
year: "2023"
relatedSessions:
  - title: "Analyze hangs with Instruments"
    code: "10248"
    description: "user interface elements often mimic real-world interactions, including real-time"
  - title: "Bring your game to Mac, Part 1: Make a game plan"
    code: "10123"
    description: "bring modern, high-end games to mac and ipad with the powerful features of metal"
  - title: "Bring your game to Mac, Part 2: Compile your shaders"
    code: "10124"
    description: "discover how the metal shader converter streamlines the process of bringing your"
  - title: "Bring your game to Mac, Part 3: Render with Metal"
    code: "10125"
    description: "discover how you can support metal in your rendering code as we close out our th"
---

# Beyond the basics of structured concurrency

---

## Highlight

> Session 以"厨房做汤"为贯穿全场的例子，系统讲解结构化并发的三个核心机制：任务取消（Task Cancellation）、任务层级（Task Hierarchy）和任务组（Task Groups）的资源管理。

---

## 核心内容

Session 以"厨房做汤"为贯穿全场的例子，系统讲解结构化并发的三个核心机制：任务取消（Task Cancellation）、任务层级（Task Hierarchy）和任务组（Task Groups）的资源管理。

结构化任务通过 `async let` 和 Task Group 创建，它们与父任务形成树状层级关系。非结构化任务（`Task.init` 和 `Task.detached`）不参与这个层级。结构化任务的生命周期绑定到声明它们的作用域，离开作用域时自动取消。

**任务取消**是协作式的——取消只设置标志位，不会强制停止。你的代码需要主动检查 `Task.isCancelled` 或调用 `Task.checkCancellation()`（后者在已取消时直接抛出错误）。关键点在于：取消父任务会递归取消所有子任务。

**`withTaskCancellationHandler`** 处理了一个微妙场景：当任务挂起等待 AsyncSequence 的下一个元素时，无法通过轮询检查取消状态。取消处理器让你在取消发生时立即执行清理逻辑，比如停止驱动 AsyncSequence 的状态机。但要注意，取消处理器与主逻辑共享状态，需要用原子操作（Swift Atomics）、锁或 Dispatch Queue 来保护。

Session 还讨论了任务组中的资源管理——如何限制并发任务数量、如何在任务组中使用 task-local values 进行追踪和性能分析。


### 最佳实践

- 在每个耗时操作前检查取消状态，避免做无用功
- 使用 `withTaskCancellationHandler` 处理 AsyncSequence 等挂起场景下的取消
- 取消处理器中的共享状态必须用原子操作或锁保护，不能用 Actor
- 优先使用结构化任务（`async let` 和 Task Group），享受自动取消传播
- 在服务端环境中，利用 task-local values 实现请求级别的追踪和日志关联

---

## 相关 Session

- [Analyze hangs with Instruments](/articles?year=2023&topic=all&search=10248) — user interface elements often mimic real-world interactions, including real-time
- [Bring your game to Mac, Part 1: Make a game plan](/articles?year=2023&topic=all&search=10123) — bring modern, high-end games to mac and ipad with the powerful features of metal
- [Bring your game to Mac, Part 2: Compile your shaders](/articles?year=2023&topic=all&search=10124) — discover how the metal shader converter streamlines the process of bringing your
- [Bring your game to Mac, Part 3: Render with Metal](/articles?year=2023&topic=all&search=10125) — discover how you can support metal in your rendering code as we close out our th
