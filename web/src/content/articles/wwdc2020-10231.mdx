---
title: "Reduce latency with HLS Blocking Playlist Reload"
description: "blocking playlist reload is a required component of low-latency hls that improves segment discovery time in live streams and addresses the common problem of stale playlists when delivering through an "
date: 2026-06-10
tags: ["WWDC 2020"]
thumbnail: "/images/sessions/2020/10231.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2020/10231/"
sessionId: "wwdc2020-10231"
year: "2020"
relatedSessions:
  - title: "Adapt ad insertion to Low-Latency HLS"
    code: "10232"
    description: "find out how to integrate advertising into your low-latency hls streams through "
  - title: "Author fragmented MPEG-4 content with AVAssetWriter"
    code: "10011"
    description: "transform your audio and video content into fragmented mpeg-4 files for a faster"
  - title: "Build Metal-based Core Image kernels with Xcode"
    code: "10021"
    description: "learn how to integrate and load core image kernels written in the metal shading "
  - title: "Deliver a better HLS audio experience"
    code: "10158"
    description: "discover techniques for streaming high-quality audio to bandwidth-limited networ"
---

# Reduce latency with HLS Blocking Playlist Reload

---

## Highlight

> 深入了解 Reduce latency with HLS Blocking Playlist Reload 的技术细节和最佳实践。

---

## 核心内容

这场 Session 深入讲解了 Low-Latency HLS 的 Blocking Playlist Reload 机制。在标准 HLS 中，播放器通过定期重新请求 playlist 来发现新内容。播放器加载 playlist 后，需要等待 `target duration` 的一半时间才能再次请求。这种轮询模式在直播场景下造成了两方面的浪费：一是延迟（等了太长时间才拿到新数据），二是带宽（请求了太多次拿到的是旧数据）。

Blocking Playlist Reload 的解决方案是：当播放器重新请求 playlist 时，如果服务器判断 playlist 还没有更新，它不会立即返回旧数据，而是保持 HTTP 连接打开，等到 playlist 真正更新后再返回。这样播放器不需要猜测轮询间隔，新数据产生后第一时间就能拿到。

演讲者详细讲解了 `#EXT-X-SERVER-CONTROL` 标签中的 `CAN-BLOCK-RELOAD=YES` 声明，以及客户端如何通过 `_HLS_msn` 和 `_HLS_part` 查询参数指定自己期望的 Media Sequence Number。服务端收到请求后，如果指定的 MSN 还没准备好，就 blocking 等待。Session 还讨论了超时处理、错误恢复和多码率场景下的 playlist 同步问题。


### 最佳实践

- 服务端 blocking 超时建议设为 `PART-HOLD-BACK` 值，通常 3 秒，避免连接占用时间过长
- CDN 必须支持 HTTP/2 的长连接，否则 blocking 请求会被中间代理截断
- 监控服务端的并发 blocking 连接数，这是容量规划的核心指标
- 客户端在网络切换（Wi-Fi 到蜂窝）后应重置 blocking 状态，重新请求完整 playlist
- 如果 blocking 请求超时，客户端应回退到传统轮询模式作为降级策略

---

## 相关 Session

- [Adapt ad insertion to Low-Latency HLS](/articles?year=2020&topic=all&search=10232) — find out how to integrate advertising into your low-latency hls streams through 
- [Author fragmented MPEG-4 content with AVAssetWriter](/articles?year=2020&topic=all&search=10011) — transform your audio and video content into fragmented mpeg-4 files for a faster
- [Build Metal-based Core Image kernels with Xcode](/articles?year=2020&topic=all&search=10021) — learn how to integrate and load core image kernels written in the metal shading 
- [Deliver a better HLS audio experience](/articles?year=2020&topic=all&search=10158) — discover techniques for streaming high-quality audio to bandwidth-limited networ
