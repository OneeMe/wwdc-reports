---
title: "Bring Core Data concurrency to Swift and SwiftUI"
description: "discover how core data is adopting the new concurrency capabilities of swift 5.5, leading to more concise, efficient, and safe asynchronous code. we"
date: 2026-06-10
tags: ["WWDC 2021"]
thumbnail: "/images/sessions/2021/10017.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2021/10017/"
sessionId: "wwdc2021-10017"
year: "2021"
relatedSessions:
  - title: "ARC in Swift: Basics and beyond"
    code: "10216"
    description: "learn about the basics of object lifetimes and arc in swift. dive deep into what"
  - title: "Discover concurrency in SwiftUI"
    code: "10019"
    description: "discover how you can use swift"
  - title: "Explore structured concurrency in Swift"
    code: "10134"
    description: "when you have code that needs to run at the same time as other code, it"
  - title: "Meet AsyncSequence"
    code: "10058"
    description: "iterating over a sequence of values over time is now as easy as writing a “for” "
---

# Bring Core Data concurrency to Swift and SwiftUI

---

## Highlight

> Core Data 的并发模型一直是 iOS 开发中最容易踩坑的部分之一。线程安全规则很严格：`NSManagedObject` 绑定到创建它的 `NSManagedObjectContext`，跨线程访问会直接 crash。iOS 15 中，Apple 为 Core Data 加入了原生 Swift Concurrency 支持，让并发代码的编写变得更安全。

---

## 核心内容

Core Data 的并发模型一直是 iOS 开发中最容易踩坑的部分之一。线程安全规则很严格：`NSManagedObject` 绑定到创建它的 `NSManagedObjectContext`，跨线程访问会直接 crash。iOS 15 中，Apple 为 Core Data 加入了原生 Swift Concurrency 支持，让并发代码的编写变得更安全。

Session 展示了如何用 `@MainActor` 标注 View Context，这样在 SwiftUI 视图中访问 Core Data 对象时，编译器会自动保证线程安全。对于后台操作，`NSManagedObjectContext` 新增了 `performAndWait` 和 `perform` 的 async 版本，配合 `@Sendable` 闭包，可以在 async 上下文中安全地执行数据库操作。

另外还讨论了 `NSFetchedResultsController` 在 SwiftUI 中的替代方案——`@FetchRequest` 配合 `@SectionedFetchRequest` 可以实现分组列表的数据绑定，性能和 UIKit 版本相当。


### 最佳实践

迁移到 Swift Concurrency 时，不要一次性改写所有 Core Data 代码。先从新写的功能开始用 async/await，旧的 completion-based 代码可以逐步迁移。两者的混合使用是安全的，因为 `context.perform` 的 async 版本内部只是对原有 `performAndWait` 的封装。

如果你使用 `NSPersistentCloudKitContainer`，注意 CloudKit 的同步操作会在后台 context 中执行。不要在 View Context 的 `objectsWillChange` 通知中做繁重的计算——这个通知可能在主线程上高频触发。

---

## 相关 Session

- [ARC in Swift: Basics and beyond](/articles?year=2021&topic=all&search=10216) — learn about the basics of object lifetimes and arc in swift. dive deep into what
- [Discover concurrency in SwiftUI](/articles?year=2021&topic=all&search=10019) — discover how you can use swift
- [Explore structured concurrency in Swift](/articles?year=2021&topic=all&search=10134) — when you have code that needs to run at the same time as other code, it
- [Meet AsyncSequence](/articles?year=2021&topic=all&search=10058) — iterating over a sequence of values over time is now as easy as writing a “for” 
