---
title: "Perform accessibility audits for your app"
description: "discover how you can test your app for accessibility with every build. learn how to perform automated audits for accessibility using xctest and find out how to interpret the results. we"
date: 2026-06-10
tags: ["WWDC 2023"]
thumbnail: "/images/sessions/2023/10035.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2023/10035/"
sessionId: "wwdc2023-10035"
year: "2023"
relatedSessions:
  - title: "Analyze hangs with Instruments"
    code: "10248"
    description: "user interface elements often mimic real-world interactions, including real-time"
  - title: "Beyond the basics of structured concurrency"
    code: "10170"
    description: "it"
  - title: "Bring your game to Mac, Part 1: Make a game plan"
    code: "10123"
    description: "bring modern, high-end games to mac and ipad with the powerful features of metal"
  - title: "Bring your game to Mac, Part 2: Compile your shaders"
    code: "10124"
    description: "discover how the metal shader converter streamlines the process of bringing your"
---

# Perform accessibility audits for your app

---

## Highlight

> Session 介绍了 Xcode 15 中无障碍审计的自动化能力，以及如何正确设置无障碍属性让审计和 UI 测试同时受益。

---

## 核心内容

Session 介绍了 Xcode 15 中无障碍审计的自动化能力，以及如何正确设置无障碍属性让审计和 UI 测试同时受益。

**自动化无障碍审计**。以前你需要在 Accessibility Inspector 中手动运行审计，现在可以在 UI 测试中调用 `performAccessibilityAudit()`。它会检查当前视图的常见无障碍问题：元素描述缺失、标签不可读、对比度不足等。发现问题时测试自动失败，问题报告直接内联在 Xcode 源码编辑器中。

**问题的诊断与修复**。Session 用一个示例 App 展示了完整流程：审计发现两个问题——图片视图没有描述、文本视图的 accessibility label 使用了非人类可读的字符串。修复方式是把 `accessibilityLabel` 改为 `accessibilityIdentifier`（后者只影响 UI 测试定位，不影响 VoiceOver 体验），并对纯装饰性图片从 `accessibilityElements` 中排除。

**过滤误报**。`performAccessibilityAudit` 支持两个可选参数：第一个指定要审计的类别（如动态字体、对比度），第二个是一个闭包，让你决定哪些问题要报告、哪些要忽略（比如已知的误报）。

**UI 测试与无障碍的协同**。Session 强调了一个关键观点：如果你的 UI 测试能找到元素，辅助技术也能找到——因为 XCTest 依赖无障碍属性来定位元素。这意味着写好 UI 测试本身就是在做好无障碍的基础工作。


### 最佳实践

- 将 `performAccessibilityAudit()` 加入每个 UI 测试用例，CI 中自动检测无障碍回归
- 用 `accessibilityIdentifier` 而非 `accessibilityLabel` 来定位 UI 测试元素
- 纯装饰性元素不要设置无障碍属性，通过 `accessibilityElements` 控制暴露范围
- 审计发现问题时，先确认是否为误报再决定是否过滤
- 设置 `continueAfterFailure = true` 让审计报告所有问题而非停在第一个

---

## 相关 Session

- [Analyze hangs with Instruments](/articles?year=2023&topic=all&search=10248) — user interface elements often mimic real-world interactions, including real-time
- [Beyond the basics of structured concurrency](/articles?year=2023&topic=all&search=10170) — it
- [Bring your game to Mac, Part 1: Make a game plan](/articles?year=2023&topic=all&search=10123) — bring modern, high-end games to mac and ipad with the powerful features of metal
- [Bring your game to Mac, Part 2: Compile your shaders](/articles?year=2023&topic=all&search=10124) — discover how the metal shader converter streamlines the process of bringing your
