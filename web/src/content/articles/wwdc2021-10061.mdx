---
title: "Meet TextKit 2"
description: "meet textkit 2: apple"
date: 2026-06-10
tags: ["WWDC 2021"]
thumbnail: "/images/sessions/2021/10061.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2021/10061/"
sessionId: "wwdc2021-10061"
year: "2021"
relatedSessions:
  - title: "Add intelligence to your widgets"
    code: "10049"
    description: "discover how to you can add intelligence to your widgets in smart stacks. we"
  - title: "Build a workout app for Apple Watch"
    code: "10009"
    description: "build a workout app from scratch using swiftui and healthkit during this code al"
  - title: "Build apps that share data through CloudKit and Core Data"
    code: "10015"
    description: "learn how to easily build apps that share data between multiple icloud users wit"
  - title: "Craft search experiences in SwiftUI"
    code: "10176"
    description: "discover how you can help people quickly find specific content within your apps."
---

# Meet TextKit 2

---

## Highlight

> TextKit 是 iOS/macOS 上所有文本渲染的底层框架（`UILabel`、`UITextView`、`NSTextField` 都基于它）。TextKit 2 是这个框架自 iOS 7 以来的最大架构变更。

---

## 核心内容

TextKit 是 iOS/macOS 上所有文本渲染的底层框架（`UILabel`、`UITextView`、`NSTextField` 都基于它）。TextKit 2 是这个框架自 iOS 7 以来的最大架构变更。

核心概念变化：TextKit 1 使用 `NSTextStorage` -> `NSLayoutManager` -> `NSTextContainer` 的三层架构。TextKit 2 保留了 `NSTextStorage`，但用 `NSTextLayoutFragment` 替代了 `NSLayoutManager` 作为布局单元。

`NSTextLayoutFragment` 代表文本的一个布局片段（比如一个段落、一个表格单元格）。每个 fragment 可以独立计算布局和渲染，这使得多线程布局成为可能——TextKit 1 的 `NSLayoutManager` 只能在主线程工作。

Session 还介绍了 `NSTextContentManager`，它是 `NSTextStorage` 和布局层之间的协调器。如果你需要渲染非文本内容（比如内嵌图片、公式），可以通过自定义 `NSTextContentManager` 来实现。


### 最佳实践

不要急于把现有的 TextKit 1 代码迁移到 TextKit 2。iOS 15 的 TextKit 2 实现还有一些限制（比如部分 `NSAttributedString` 的 attribute 尚未完全支持）。建议只在新建的文本编辑功能中尝试 TextKit 2。

如果你需要做自定义文本布局（比如代码编辑器的高亮、Markdown 渲染），TextKit 2 的 fragment 模型比 TextKit 1 更适合。你可以为每种语法元素（关键字、注释、字符串）创建不同的 `NSTextLayoutFragment`，分别控制它们的样式。

---

## 相关 Session

- [Add intelligence to your widgets](/articles?year=2021&topic=all&search=10049) — discover how to you can add intelligence to your widgets in smart stacks. we
- [Build a workout app for Apple Watch](/articles?year=2021&topic=all&search=10009) — build a workout app from scratch using swiftui and healthkit during this code al
- [Build apps that share data through CloudKit and Core Data](/articles?year=2021&topic=all&search=10015) — learn how to easily build apps that share data between multiple icloud users wit
- [Craft search experiences in SwiftUI](/articles?year=2021&topic=all&search=10176) — discover how you can help people quickly find specific content within your apps.
