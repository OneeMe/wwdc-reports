---
title: "Explore logging in Swift"
description: "meet the latest generation of swift unified logging apis. learn how to log events and errors in your app while preserving privacy. take advantage of powerful yet readable options for formatting data —"
date: 2026-06-10
tags: ["WWDC 2020"]
thumbnail: "/images/sessions/2020/10168.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2020/10168/"
sessionId: "wwdc2020-10168"
year: "2020"
relatedSessions:
  - title: "Build an Endpoint Security app"
    code: "10159"
    description: "system extensions improve the reliability and security of macos. learn about the"
  - title: "Build trust through better privacy"
    code: "10676"
    description: "privacy is a more important issue than ever. learn about apple"
  - title: "Handle the Limited Photos Library in your app"
    code: "10641"
    description: "access the photos and videos you need for your app while preserving privacy. wit"
  - title: "Introducing Car Keys"
    code: "10006"
    description: "you can now store car keys on iphone or apple watch. you no longer have to bring"
---

# Explore logging in Swift

---

## Highlight

> 统一日志系统（Unified Logging）是 Apple 从 macOS 10.12 / iOS 10 开始引入的高性能日志框架，通过 `os` 模块在 Swift 中使用。Session 系统介绍了这套日志系统的设计理念、API 使用和最佳实践，重点介绍了 iOS 14 中新增的 `Logger` 结构体。

---

## 核心内容

统一日志系统（Unified Logging）是 Apple 从 macOS 10.12 / iOS 10 开始引入的高性能日志框架，通过 `os` 模块在 Swift 中使用。Session 系统介绍了这套日志系统的设计理念、API 使用和最佳实践，重点介绍了 iOS 14 中新增的 `Logger` 结构体。

`Logger` 是 iOS 14 新引入的 Swift-native 日志 API，取代了之前的 `os_log` 函数。它提供了类型安全的字符串插值日志、结构化的日志级别（debug/info/error/fault）和灵活的子系统/分类配置。与 `print()` 不同，`Logger` 的日志会被系统自动收集，可以通过 Console.app 或 `log` 命令行工具查看，并且有智能的持久化策略——debug 级别的日志只保存在内存中，error 和 fault 级别会写入磁盘。

Session 还讨论了日志的性能影响。统一日志系统在底层做了大量优化：日志消息的格式化是惰性的（只在日志真的被记录时才发生），字符串插值在编译期就部分完成了。这意味着即使你的代码里写了很多 `logger.debug()` 调用，在 release 构建中这些 debug 日志几乎不产生任何开销。


### 最佳实践

- 为每个模块/服务创建独立的 `Logger` 实例，使用不同的 category 便于过滤
- 在 release 构建中避免使用 debug 级别记录关键信息——它不会被持久化
- 动态数据（用户名、订单号等）默认是私有的，需要显式标记 `.public` 才会在日志中显示
- 用 Console.app 或 `log stream` 命令查看日志，不要依赖 Xcode 的控制台输出
- 日志消息要写得有上下文——"操作失败"是无效日志，"支付处理失败: 超时 30秒, 订单号 xxx"才是好日志

---

## 相关 Session

- [Build an Endpoint Security app](/articles?year=2020&topic=all&search=10159) — system extensions improve the reliability and security of macos. learn about the
- [Build trust through better privacy](/articles?year=2020&topic=all&search=10676) — privacy is a more important issue than ever. learn about apple
- [Handle the Limited Photos Library in your app](/articles?year=2020&topic=all&search=10641) — access the photos and videos you need for your app while preserving privacy. wit
- [Introducing Car Keys](/articles?year=2020&topic=all&search=10006) — you can now store car keys on iphone or apple watch. you no longer have to bring
