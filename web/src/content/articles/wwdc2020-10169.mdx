---
title: "Swift packages: Resources and localization"
description: "bring your resources along for the ride when you organize and share code using swift packages. discover how to include assets like images and storyboards in a package and how to access them from code."
date: 2026-06-10
tags: ["WWDC 2020"]
thumbnail: "/images/sessions/2020/10169.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2020/10169/"
sessionId: "wwdc2020-10169"
year: "2020"
relatedSessions:
  - title: "A conversation with Lisa Jackson and former Attorney General Eric Holder"
    code: "10970"
    description: "eric holder was the 82nd attorney general of the united states, having served fr"
  - title: "Build location-aware enterprise apps"
    code: "10140"
    description: "develop location-aware enterprise apps for your business and personalize your em"
  - title: "Create a seamless speech experience in your apps"
    code: "10022"
    description: "augment your app"
  - title: "Formatters: Make data human-friendly"
    code: "10160"
    description: "save yourself time and frustration: when you display data in your app — includin"
---

# Swift packages: Resources and localization

---

## Highlight

> Swift Package Manager 在 Swift 5.3（Xcode 12）中引入了资源文件（Resources）和本地化（Localization）的原生支持。在此之前，Swift Package 只能包含 Swift 源码文件——不能放图片、JSON 配置、Core Data 模型文件，更不能做本地化。这迫使很多开发者继续使用 CocoaPods 或手动集成。

---

## 核心内容

Swift Package Manager 在 Swift 5.3（Xcode 12）中引入了资源文件（Resources）和本地化（Localization）的原生支持。在此之前，Swift Package 只能包含 Swift 源码文件——不能放图片、JSON 配置、Core Data 模型文件，更不能做本地化。这迫使很多开发者继续使用 CocoaPods 或手动集成。

Session 详细介绍了 `Package.swift` 中新的 `resources` 声明语法。你可以使用 `.process("Resources")` 让 Xcode 自动处理资源文件，或者使用 `.copy("Configs")` 将文件原样复制到 bundle 中。前者会对 asset catalogs 和 `.strings` 文件做特殊处理，后者适用于需要保持原始目录结构的场景。

本地化方面，Swift Package 现在支持声明 `defaultLocalization`，并在 `targets` 中指定本地化资源的目录结构。`String(localized:)` API 可以直接在 Swift 代码中使用 `.strings` 和 `.stringsdict` 文件，也可以在 SwiftUI 中使用 `Text("key")` 的方式引用本地化字符串。


### 最佳实践

- 在 `Package.swift` 中声明 `defaultLocalization`，即使暂时只有一种语言——未来添加多语言时会少很多麻烦
- 使用 `.process()` 处理所有需要编译的资源类型（Asset Catalog、Storyboard、Strings），用 `.copy()` 处理原始数据文件
- 在 package 的公共 API 中不要暴露 `Bundle.module`，而是在内部封装好资源访问逻辑
- 本地化字符串文件统一使用 `.xcstrings` 格式（Xcode 15+），它比 `.strings` 更容易管理
- 测试中使用 `Bundle.module` 访问测试数据，不要把测试数据放在源码目录中

---

## 相关 Session

- [A conversation with Lisa Jackson and former Attorney General Eric Holder](/articles?year=2020&topic=all&search=10970) — eric holder was the 82nd attorney general of the united states, having served fr
- [Build location-aware enterprise apps](/articles?year=2020&topic=all&search=10140) — develop location-aware enterprise apps for your business and personalize your em
- [Create a seamless speech experience in your apps](/articles?year=2020&topic=all&search=10022) — augment your app
- [Formatters: Make data human-friendly](/articles?year=2020&topic=all&search=10160) — save yourself time and frustration: when you display data in your app — includin
