---
title: "Use async/await with URLSession"
description: "discover how you can adopt swift concurrency in urlsession using async/await and asyncsequence, and how you can apply swift concurrency concepts to improve your networking code."
date: 2026-06-10
tags: ["WWDC 2021"]
thumbnail: "/images/sessions/2021/10095.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2021/10095/"
sessionId: "wwdc2021-10095"
year: "2021"
relatedSessions:
  - title: "ARC in Swift: Basics and beyond"
    code: "10216"
    description: "learn about the basics of object lifetimes and arc in swift. dive deep into what"
  - title: "Bring Core Data concurrency to Swift and SwiftUI"
    code: "10017"
    description: "discover how core data is adopting the new concurrency capabilities of swift 5.5"
  - title: "Discover concurrency in SwiftUI"
    code: "10019"
    description: "discover how you can use swift"
  - title: "Explore structured concurrency in Swift"
    code: "10134"
    description: "when you have code that needs to run at the same time as other code, it"
---

## Highlight

> Session 讲解了 Swift 5.5 的 async/await 如何与 URLSession 集成。iOS 15 中 URLSession 新增了三个核心的 async 方法：`data(for:)`（普通请求）、`data(for:delegate:)`（带代理的请求）、`upload(for:from:)`（上传请求）。

---

## 核心内容

Session 讲解了 Swift 5.5 的 async/await 如何与 URLSession 集成。iOS 15 中 URLSession 新增了三个核心的 async 方法：`data(for:)`（普通请求）、`data(for:delegate:)`（带代理的请求）、`upload(for:from:)`（上传请求）。

这些方法直接返回 `Data` 和 `URLResponse`，不再需要 closure 回调。错误处理用 `try/catch`，任务取消用 `Task.cancel()`。Session 用多个实例对比了 callback 版本和 async/await 版本的代码复杂度，展示了新 API 如何简化常见的网络编程模式：串行请求、并行请求、请求链、重试逻辑。

还讨论了 `URLSessionTask` 在 structured concurrency 中的生命周期管理。


### 最佳实践

1. **把网络层统一封装成 async 函数**。不要在部分地方用 async/await、部分地方用 callback。统一之后可以消除所有的 completion handler 模板代码。
2. **并行请求用 `async let` 而不是 `TaskGroup`**，除非你需要动态数量的并发任务。`async let` 更直观，编译器会帮你管理生命周期。
3. **请求取消要处理 partial data**。当 `Task.cancel()` 被调用时，正在进行的网络请求不会立即中断，而是在下一次 suspend point 抛出 `CancellationError`。如果你需要保存中间结果，在 catch 块中处理。

---

## 相关 Session

- [ARC in Swift: Basics and beyond](/articles/wwdc2021-10216) — learn about the basics of object lifetimes and arc in swift. dive deep into what
- [Bring Core Data concurrency to Swift and SwiftUI](/articles/wwdc2021-10017) — discover how core data is adopting the new concurrency capabilities of swift 5.5
- [Discover concurrency in SwiftUI](/articles/wwdc2021-10019) — discover how you can use swift
- [Explore structured concurrency in Swift](/articles/wwdc2021-10134) — when you have code that needs to run at the same time as other code, it
