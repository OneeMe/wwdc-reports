---
title: "What’s new in camera capture"
description: "learn how you can interact with video effects in control center including center stage, portrait mode, and mic modes. we"
date: 2026-06-10
tags: ["WWDC 2021"]
thumbnail: "/images/sessions/2021/10047.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2021/10047/"
sessionId: "wwdc2021-10047"
year: "2021"
relatedSessions:
  - title: "Build dynamic iOS apps with the Create ML framework"
    code: "10037"
    description: "discover how your app can train core ml models fully on device with the create m"
  - title: "Capture high-quality photos using video formats"
    code: "10247"
    description: "your app can take full advantage of the powerful camera systems on iphone by usi"
  - title: "Explore low-latency video encoding with VideoToolbox"
    code: "10158"
    description: "supporting low latency encoders has become an important aspect of video applicat"
  - title: "Improve access to Photos in your app"
    code: "10046"
    description: "phpicker is the simplest and most secure way to integrate the photos library int"
---

# What’s new in camera capture

---

## Highlight

> AVFoundation 的相机捕获能力在 iOS 15 得到了全面升级。Session 覆盖了三个主要方向：视频格式扩展、景深数据增强和 API 现代化。

---

## 核心内容

AVFoundation 的相机捕获能力在 iOS 15 得到了全面升级。Session 覆盖了三个主要方向：视频格式扩展、景深数据增强和 API 现代化。

视频格式方面，iPhone 13 Pro 系列支持了 ProRes 视频录制。ProRes 是专业后期制作的标准格式，之前需要外接录机才能实现。现在通过 AVFoundation 可以直接录制 ProRes 422 和 ProRes 422 HQ 到 iPhone 存储中。

景深数据方面，iOS 15 引入了 Disparity Output（视差输出），这是 Cinematic 模式的核心数据。你可以获取每一帧的深度图，用于自定义的景深效果或者背景虚化。`AVDepthData` 现在支持更高精度的视差数据。

API 现代化方面，`AVCaptureVideoDataOutput` 的帧回调现在可以用 `AsyncSequence` 的方式消费，不再需要 delegate 回调。这让视频处理管线可以用 async/await 写成线性的代码流。


### 最佳实践

ProRes 文件体积巨大，录制前检查可用存储空间。一个 5 分钟的 4K ProRes 片段可能超过 10GB。建议在录制开始前用 `URL.resourceValues(forKeys:)` 检查剩余空间，不足时提示用户。

使用 `AsyncSequence` 处理帧时，在 `for await` 循环中加入 `Task.isCancelled` 检查。当用户停止录制时，外部 Task 被取消，循环应该及时退出而不是处理完缓冲区中的所有帧。

---

## 相关 Session

- [Build dynamic iOS apps with the Create ML framework](/articles?year=2021&topic=all&search=10037) — discover how your app can train core ml models fully on device with the create m
- [Capture high-quality photos using video formats](/articles?year=2021&topic=all&search=10247) — your app can take full advantage of the powerful camera systems on iphone by usi
- [Explore low-latency video encoding with VideoToolbox](/articles?year=2021&topic=all&search=10158) — supporting low latency encoders has become an important aspect of video applicat
- [Improve access to Photos in your app](/articles?year=2021&topic=all&search=10046) — phpicker is the simplest and most secure way to integrate the photos library int
