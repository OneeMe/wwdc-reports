---
title: "What’s new in SwiftData"
description: "discover the latest enhancements to swiftdata. we'll show you how to persist custom and third-party types using codable, and group fetched data into sections in your swiftui app. we'll also explore ho"
date: 2026-06-10
tags: ["SwiftData", "ModelResultsObserver", "Codable", "SwiftUI", "Observation"]
thumbnail: "/images/sessions/2026/274.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/274/"
sessionId: "wwdc2026-274"
year: "2026"
relatedSessions:
  - title: "Build, deliver, and automate with Xcode Cloud"
    code: "261"
    description: "discover the latest updates to xcode cloud that quickly get you started building"
  - title: "Code-along: Add persistence with SwiftData"
    code: "275"
    description: "experience swiftdata in action as we add persistence to an existing app. we'll s"
  - title: "Discover container machines"
    code: "389"
    description: "meet container machines, a new tool included in container that offers a lightwei"
  - title: "Get the most out of Device Hub"
    code: "260"
    description: "learn how device hub can accelerate your development workflows. we'll take a tou"
---

# What’s new in SwiftData

---

## Highlight

> 深入了解 What’s new in SwiftData 的技术细节和最佳实践。

---

## 核心内容

过去两年用 SwiftData 的人都有个痛点：`@Query` 死死绑在 SwiftUI 视图上。如果你想在非 UI 层（比如一个负责计算地图边界的 Controller，或者一个后台同步服务）监听数据变化，只能自己手写一堆 `NotificationCenter` 或者硬凑一个隐藏的 SwiftUI 视图。Apple 这次算是向现实妥协了，推出了 `ModelResultsObserver` 和 `HistoryObserver`，把查询和监听能力下放给了普通的 Swift 代码。

除了架构层面的解绑，这次还补了几个实用的语法糖。`@Query` 终于原生支持 `sectionBy` 分组了，写带 Section 的 List 不用再自己手写 Dictionary 分组逻辑。针对 MapKit 这种第三方库里的复杂类型，新增了 `@Attribute(.codable)` 作为逃生舱，直接序列化存库，省去了写 Transformable 的麻烦。

这对采用 MVVM 架构或者在 App 里混合使用 UIKit/SwiftUI 的团队影响最大。你终于可以把数据获取逻辑从 View 层抽离到 ViewModel 里，让 View 只做纯粹的渲染。


### 最佳实践

- **已有项目的迁移策略**：如果你之前为了在非 UI 层监听 SwiftData 数据，写了基于 `NSManagedObjectContextDidSave` 的 Notification 监听，或者搞了个隐藏的 SwiftUI View 来桥接 `@Query`，现在可以全部删掉，替换为 `ModelResultsObserver`。这能让你的 ViewModel 或 Controller 代码干净一半。
- **新项目的采用建议**：大胆把数据获取逻辑从 View 层抽离。View 层的 `@Query` 只用于最简单的列表渲染；涉及复杂计算、网络请求联动、地图边界计算的场景，统统交给持有 `ModelResultsObserver` 的 `@Observable` 状态对象。
- **实战中容易踩的坑**：使用 `HistoryObserver` 做云端同步时，一定要在初始化时传入 `authors` 参数（比如 `["App"]`）。如果不加过滤，你会把从服务器拉取下来的数据变更再次触发同步，直接导致死循环。

---

## 相关 Session

- [Build, deliver, and automate with Xcode Cloud](/articles?year=2026&topic=all&search=261) — discover the latest updates to xcode cloud that quickly get you started building
- [Code-along: Add persistence with SwiftData](/articles?year=2026&topic=all&search=275) — experience swiftdata in action as we add persistence to an existing app. we'll s
- [Discover container machines](/articles?year=2026&topic=all&search=389) — meet container machines, a new tool included in container that offers a lightwei
- [Get the most out of Device Hub](/articles?year=2026&topic=all&search=260) — learn how device hub can accelerate your development workflows. we'll take a tou
