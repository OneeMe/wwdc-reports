---
title: "Go further with Metal 4 games"
description: "dive deeper into the latest advancements in metal 4. we"
date: 2026-06-10
tags: ["AVFoundation", "音视频", "相机", "媒体播放", "SwiftUI"]
thumbnail: "/images/sessions/2025/211.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2025/211/"
sessionId: "wwdc2025-211"
year: "2025"
relatedSessions:
  - title: "Bring your SceneKit project to RealityKit"
    code: "288"
    description: "understand scenekit deprecation and explore how to transition your 3d projects t"
  - title: "Combine Metal 4 machine learning and graphics"
    code: "262"
    description: "learn how to seamlessly combine machine learning into your graphics applications"
  - title: "Discover Metal 4"
    code: "205"
    description: "learn how to get started leveraging the powerful new features of metal 4 in your"
  - title: "Engage players with the Apple Games app"
    code: "215"
    description: "meet the games app – a new destination for players to keep up with what"
---

# Go further with Metal 4 games

---

## Highlight

> 深入了解 Go further with Metal 4 games 的技术细节和最佳实践。

---

## 核心内容

AVFoundation 这次的更新有一个非常清晰的主线：把 SwiftUI 从音视频开发的"二等公民"里捞出来。`CameraPreview` 是最直接的信号——一个原生 SwiftUI 视图，接收 `AVCaptureSession` 就完事了，不用再写那段每次都要写、每次都在细节上出错的 `UIViewRepresentable` 包装器。播放器那边也是同样的思路，`VideoPlayer` 终于开放了控件自定义，进度条、播放按钮、音量控制全都可以用 SwiftUI 视图替换，不再被系统默认样式绑架。

另一个值得关注的方向是"智能化"。`AVAssetExportSession` 现在能根据目标平台自动匹配编码参数，不用你自己调 `AVVideoCodecKey` 和 `AVVideoCompressionProperties`。`AVAudioRecorder` 新增了语音活动检测（VAD），检测到人声才写入数据，安静的时候不占存储。智能章节功能更激进——直接调用 Apple Intelligence 分析音视频内容，自动生成带缩略图的章节导航。

音频引擎那边，`AVAudioEngine` 支持运行时动态插入和移除处理节点了，不用重建整条音频图。空间音频也开放了自定义 HRTF（Head-Related Transfer Function），游戏和 VR 应用可以基于用户头部模型做更精准的 3D 定位。多流捕获则让前后摄像头同时以不同分辨率输出成为内置能力，双机位录制不再是专业设备的专利。


### 最佳实践

先检查项目的最低部署版本。如果还是 iOS 17/18，`CameraPreview` 和播放器自定义控件都用不了，这场 Session 的价值在于"了解新 API、为未来做准备"，不要急着重构。

如果最低版本已经是 iOS 19，`CameraPreview` 的迁移优先级最高——删掉 `UIViewRepresentable` 包装器能直接减少 30-50 行维护成本最高的代码。播放器控件自定义排第二，但迁移前先回答一个问题：播放器需求到底是"长得不一样"还是"行为不一样"？如果只是换个皮肤，插件式 API 够用；如果要加手势、弹幕、画中画自定义，继续用 `AVPlayerLayer` 自己搭更合适。

VAD 和智能章节属于锦上添花，不是必选项。VAD 适合语音备忘录、会议记录这类"录音为主"的场景，短视频 App 没必要碰。智能章节依赖 Apple Intelligence，离线不可用，如果用户经常在弱网环境使用，需要设计降级方案——比如用 `AVAsset` 的元数据做简单的静音检测来切分章节，粗糙但够用。

---

## 相关 Session

- [Bring your SceneKit project to RealityKit](/articles?year=2025&topic=all&search=288) — understand scenekit deprecation and explore how to transition your 3d projects t
- [Combine Metal 4 machine learning and graphics](/articles?year=2025&topic=all&search=262) — learn how to seamlessly combine machine learning into your graphics applications
- [Discover Metal 4](/articles?year=2025&topic=all&search=205) — learn how to get started leveraging the powerful new features of metal 4 in your
- [Engage players with the Apple Games app](/articles?year=2025&topic=all&search=215) — meet the games app – a new destination for players to keep up with what
