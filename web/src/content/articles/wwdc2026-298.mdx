---
title: "Meet the Evaluations framework"
description: "learn how to evaluate model-driven experiences using the evaluations framework. in a probabilistic world, unit tests alone won't suffice. discover how to define metrics, automatically grade outputs, and aggregate statistics to ensure your ai-powered features perform reliably across apple's platforms."
date: 2026-06-10
tags: ["Evaluations", "Foundation Models", "Swift Testing", "Generative AI", "Model Judge"]
thumbnail: "/images/sessions/2026/298.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/298/"
sessionId: "wwdc2026-298"
year: "2026"
relatedSessions:
  - title: "Create robust evaluations for agentic apps"
    code: "299"
    description: "learn how to leverage advanced features of the evaluations framework to build robust evaluations for your app"
  - title: "Build agentic app experiences with the Foundation Models framework"
    code: "242"
    description: "learn how to take your intelligence features further with foundation models framework primitives"
  - title: "Improve your prompts by hill climbing with Evaluations"
    code: "246"
    description: "explore how to use the evaluations framework to improve your prompts and intelligent features"
  - title: "Bring an LLM provider to the Foundation Models framework"
    code: "339"
    description: "extend the foundation models framework by implementing a languagemodelexecutor for your own llm"
---

## Highlight

> 写传统代码，输入 A 必然输出 B，XCTest 跑个断言就完事了。但接入 Foundation Models 后，模型是概率性的——同样的书评可能生成 3 个标签也可能生成 9 个。Evaluations 框架让你定义 Metric 和 Evaluator，跑几千个 `ModelSample`，然后看聚合统计（比如"80% 的情况下标签数量在 3-8 个之间"），把"概率性代码"的测试拉到了工程化的及格线。

---

## 核心内容

### 1. 这场演讲解决什么问题

传统软件的测试基于一个基本假设：**相同输入必然产生相同输出**。这个假设在生成式 AI 面前彻底失效了：

- 同一篇书评，模型这次生成 5 个标签，下次可能生成 8 个。
- 单元测试的 `XCTAssertEqual` 在这种场景下天天飘红。
- 靠人肉看几个 Demo 就发版，上线后用户投诉质量不稳定。

Evaluations 框架的核心思路是：**不追求 100% 匹配，而是定义可接受的统计分布，用大量样本验证模型表现是否达标**。

### 2. 定义你的第一个 Evaluation

Evaluation 是一个遵循 `Evaluation` 协议的结构体：

```swift
import Evaluations

struct BookTaggingEvaluation: Evaluation {
    // 数据集、评估器、聚合指标都在这里定义
}
```

配合 Swift Testing 运行，并设置优化目标：

```swift
@Test("Book Tag Evaluations", .evaluates(evaluation, info: evaluationInfo))
func evaluateBookTagging() async throws {
    let result = EvaluationContext.current.result
    let rangeMetric = BookTagEvaluationTests.evaluation.tagCount
    #expect(result.aggregateValue(.mean(of: rangeMetric)) >= 0.8)
}
```

关键点：

- Evaluation 和 Swift Testing 深度集成，跑在标准的测试流程里。
- 优化目标（optimization target）定义了"什么样的结果算通过"。
- 这里要求 tagCount 指标的均值通过率不低于 80%。

### 3. 定义 Metric 和 Evaluator

Evaluator 是具体的评判逻辑，Metric 是你要追踪的指标：

```swift
let tagCount = Metric("TagCount")

var evaluators: Evaluators {
    // 标签数量是否在 3-8 的范围内
    Evaluator { _, subject in
        let count = subject.value.tags.count
        if (count >= 3 && count <= 8) {
            return tagCount.passing(rationale: "\(count) tags")
        }
        return tagCount.failing(rationale: "Got \(count) tags, expected 3-8")
    }
}
```

再来两个更复杂的 Evaluator：

```swift
let wordCount = Metric("WordCount")
let hasGenreTag = Metric("HasGenreTag")

var evaluators: Evaluators {
    // 标签不能是多词短语
    Evaluator { _, subject in
        for tag in subject.value.tags {
            if tag.contains(" ") {
                return wordCount.failing(rationale: "Tag \(tag) contains multiple words")
            }
        }
        return wordCount.passing()
    }

    // 至少有一个标签是已知流派
    Evaluator { _, subject in
        let tags = subject.value.tags.map { $0.lowercased() }
        let knownGenres = await BookTaggingService.knownGenres
        for tag in tags {
            if knownGenres.contains(tag) {
                return hasGenreTag.passing(rationale: "Matched \(tag)")
            }
        }
        return hasGenreTag.failing()
    }
}
```

关键点：

- 每个 Evaluator 返回 `passing()` 或 `failing()`，并附带 `rationale`（理由）。
- Evaluator 可以是异步的（`await`），可以访问外部服务或数据库。
- 多个 Evaluator 独立运行，互不影响。

### 4. 聚合统计

单个样本的通过/失败不够，需要看整体分布：

```swift
let tagCount = Metric("TagCount")
let tagTotal = Metric("TagTotal")

func aggregateMetrics(using aggregator: inout MetricsAggregator) {
    aggregator.computeMean(of: tagCount)
    aggregator.group("Distribution of Tag Totals") { aggregator in
        aggregator.computeStandardDeviation(of: tagTotal)
        aggregator.computeMean(of: tagTotal)
        aggregator.computeVariance(of: tagTotal)
    }
}
```

支持的聚合操作包括：

- `computeMean`：平均值（通过率）
- `computeStandardDeviation`：标准差（稳定性）
- `computeVariance`：方差
- `group`：把相关指标分组展示

### 5. 构建数据集

Evaluation 的质量取决于数据集的质量。有两种方式构建：

**手动定义：**

```swift
var dataset = ArrayLoader(samples: [
    ModelSample(prompt: "okay I am OBSESSED and I need everyone to read this RIGHT NOW...",
                expected: BookTags(tags: ["classic", "romance", "wit", "regency"])),
    ModelSample(prompt: "Read this in one sitting between midnight and 4am and I cannot...",
                expected: BookTags(tags: ["classic", "gothic", "horror", "vampire", "suspense"])),
])
```

**从现有数据转换：**

```swift
var dataset = ArrayLoader(samples:
    Book.sampleBooks.map { book in
        ModelSample(prompt: book.review, expected: BookTags(tags: book.tags))
    }
)
```

### 6. 用 SampleGenerator 合成数据

13 个手动样本远远不够覆盖真实世界的多样性。`SampleGenerator` 可以用大模型自动生成更多样本：

```swift
let samples: [ModelSample<String>] = [
    ModelSample(prompt: "The largest planet in our solar system...", expected: "Jupiter."),
    ModelSample(prompt: "The capital of Thailand...", expected: "Bangkok."),
    ModelSample(prompt: "Swift is...", expected: "a powerful programming language."),
]

for try await sample in samples.makeSamples(
    """
    Generate diverse sentence completions about the listed topics:
      - The Solar System
      - World Capitals
    """,
    targetCount: 1000) {
    samples.append(sample)
}
```

关键点：

- `makeSamples` 需要种子数据和生成提示词。
- `targetCount` 是最终数据集大小（包含种子数据）。
- 生成的样本需要人工 review 或自动验证，防止垃圾数据污染数据集。

### 7. 用 @Guide 约束模型输出

在定义模型输出结构时，用 `@Guide` 给每个字段加约束：

```swift
@Generable
struct BookTags: Codable {
    @Guide(description: "Descriptive tags capturing themes, genres, moods, and topics from the summary",
           .count(3...8))
    var tags: [String]
}
```

关键点：

- `@Generable` 让结构体可以被模型生成。
- `@Guide` 的 `.count(3...8)` 直接约束数组长度。
- 约束越具体，模型输出越稳定，Evaluator 的通过率越高。

### 8. Model Judge：用模型评估模型

有些质量维度无法用量化的代码判断，比如"标签是否贴切"。这时候可以用 Model Judge：

```swift
ModelJudgeEvaluator(
    "TagQuality",
    scale: .numeric([
        4: "Tags are relevant and helpful for browsing",
        3: "Mostly relevant, one tag too vague or generic",
        2: "Several tags are wrong or generic",
        1: "Unhelpful or irrelevant"
    ]),
    judge: PrivateCloudComputeLanguageModel()
)
```

更精细的做法是拆分成多个评分维度：

```swift
ScoreDimension(
    "Relevance",
    description: """
        Whether each tag describes a quality, theme, or tone
        of the book itself rather than incidental details or
        the reader's personal reactions.
        """,
    scale: .numeric([
        4: "Every tag describes the book itself",
        3: "Most tags describe the book",
        2: "Some tags describe personal reactions",
        1: "Tags don't meaningfully describe the book"
    ])
)

// 在 Evaluation 中使用多维度评分
var evaluators: Evaluators {
    ModelJudgeEvaluator(
        judge: PrivateCloudComputeLanguageModel(),
        dimensions: [relevance, usefulness]
    )
}
```

给 Model Judge 添加上下文信息：

```swift
ModelJudgeEvaluator(
    judge: PrivateCloudComputeLanguageModel(),
    dimensions: [relevance, usefulness],
    prompt: ModelJudgePrompt(
        instructions: """
            You are evaluating tags generated for a personal book-tracking app where users
            organize their library by browsing and filtering tags.
            """,
        evaluationTarget: { value in
            "\(value.tags.count) Generated tags: " + value.tags.joined(separator: ", ")
        },
        reference: { input, _ in
            let expectedTags = input.expected?.tags.joined(separator: ", ")
            return ["Expected Tags": expectedTags ?? "No expected tags defined"]
        }
    )
)
```

关键点：

- Model Judge 调用成本高且慢，好钢用在刀刃上。
- 能用代码算的定量指标（字数、正则匹配、枚举值校验），绝对不要用 Model Judge。
- 多维度评分比单一总分更有诊断价值，能定位具体问题。
- 给 Judge 足够的上下文（App 场景、评估目标、参考数据），评分质量会大幅提升。

### 9. 迭代优化（Hill Climbing）

Evaluations 的核心工作流是"爬山式优化"：

1. 运行 Evaluation，看报告
2. 根据 failing rationale 修改 Prompt 或 `@Guide`
3. 再跑 Evaluation，验证改进
4. 重复直到指标达标

```swift
let instructions = Instructions {
    """
    You are a librarian and literary analyst. Given a reader's
    freeform summary of a book they read — describing their
    thoughts, feelings, and what stood out — generate a set of
    descriptive tags reflected in the summary.

    Rules:
     - Return between 3 and 8 tags.
     - Tags should be lowercase, concise (single word or hyphenated), and descriptive.
     - Tags should include the book's genre, chosen from the included list of known genres.

    Known Genres:
     - \(Self.knownGenres.joined(separator: ", "))
    """
}
```

---

## 最佳实践

- **已有项目迁移**：如果你现在是用 Playground 或者写个死循环 `for` 跑几十次肉眼看 AI 输出，立刻停下来。挑出 20-30 个最具代表性的真实用户输入（包含极端 case），用 `ModelSample` 包装起来，写你的第一个 `Evaluation`。

- **新项目采用**：采用"评估驱动开发"。在写 `@Generable` 和 Prompt 之前，先定义好 Metric 和 Evaluator。把 Xcode 的 Evaluation Report 当作你的 Dashboard，不要凭直觉改代码，让报告里的 failing rationale 驱动你的修改。

- **实战中容易踩的坑**：
  - 能用代码算的定量指标，绝对不要用 Model Judge。Judge 调用成本高且慢。
  - 数据集质量决定评估质量。13 个样本的结果可能完全误导，至少准备 100+ 样本。
  - `@Guide` 约束要具体。"生成好的标签"不如"生成 3-8 个小写单字标签，必须包含已知流派"。
  - 不要把整个业务数据集丢进 Evaluation，选有代表性的边界 case 和常见 case。

---

## 相关 Session

- [Create robust evaluations for agentic apps](/articles/wwdc2026-299) — Evaluations 框架的高级特性，包括 Tool Calling 评估和合成数据验证
- [Build agentic app experiences with the Foundation Models framework](/articles/wwdc2026-242) — 用 Foundation Models 构建 Agent 工作流
- [Improve your prompts by hill climbing with Evaluations](/articles/wwdc2026-246) — 用 Evaluations 框架迭代优化 Prompt
- [Bring an LLM provider to the Foundation Models framework](/articles/wwdc2026-339) — 把第三方 LLM 接入 Foundation Models 框架
