---
title: "Meet SwiftUI spatial layout"
description: "explore new tools for building spatial experiences using swiftui. learn the basics of 3d swiftui views on visionos, customize existing layouts with depth alignments, and use modifiers to rotate and po"
date: 2026-06-10
tags: ["Design", "空间计算", "SwiftUI"]
thumbnail: "/images/sessions/2025/273.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2025/273/"
sessionId: "wwdc2025-273"
year: "2025"
relatedSessions:
  - title: "Better together: SwiftUI and RealityKit"
    code: "274"
    description: "discover how to seamlessly blend swiftui and realitykit in visionos 26. we"
  - title: "Build a SwiftUI app with the new design"
    code: "323"
    description: "explore the ways liquid glass transforms the look and feel of your app. discover"
  - title: "Build a UIKit app with the new design"
    code: "284"
    description: "update your uikit app to take full advantage of the new design system. we"
  - title: "Explore prompt design & safety for on-device foundation models"
    code: "248"
    description: "design generative ai experiences that leverage the strengths of the foundation m"
---

# Meet SwiftUI spatial layout

---

## Highlight

> Trevor 用一个机器人收藏 app（BOT-anist）串讲了四个新工具：

---

## 核心内容

Trevor 用一个机器人收藏 app（BOT-anist）串讲了四个新工具：

**3D 视图基础**：visionOS 上所有视图都是 3D 的——SwiftUI 除了计算 width/height/X/Y，还会计算 depth 和 Z position。Model3D 有固定三维尺寸（类似 2D 的 Image），RealityView 和可调整大小的 Model3D 弹性填充可用深度（类似 2D 的 Color）。Window 提供固定深度提案，Volume 的深度可调整。ZStack 组合子视图的深度（类似 VStack 组合高度）。

**Depth Alignments**：新增深度对齐维度，与水平/垂直对齐一样直觉。默认 depth alignment 是 .back（子视图后沿对齐）。可选 .front（前沿对齐，适合让说明卡片在模型前方可见）、.center。支持自定义 DepthAlignmentID——示例实现了"深度领奖台"效果，第一名最靠前，第二名中间，第三名最后。

**rotation3DLayout**：与 rotation3DEffect 的关键区别——rotation3DLayout 修改布局系统中视图的 frame，让父布局感知旋转后的几何。rotation3DEffect 只是视觉效果，不影响布局，HStack 不知道你的火箭已经旋转了 90 度会撞到旁边的卡片。rotation3DLayout 生成的 bounding box 是轴对齐的，紧密包裹旋转后的 frame。

**SpatialContainer 和 spatialOverlay**：将多个视图放置在同一 3D 空间中（不像 ZStack 沿 Z 轴堆叠）。SpatialContainer 支持 3D 对齐——所有子视图按指定的 3D 对齐指南排列。spatialOverlay 是单视图版本——示例中用它在机器人底部叠加选中光环，两者共享同一个 3D 空间。


### 最佳实践

1. **用 debugBorder3D 可视化 3D frame**。在开发 3D 布局时，给视图加 3D 边框帮助理解布局系统实际分配的空间。本文末尾的代码就是一个完整的实现。

2. **区分 rotation3DEffect 和 rotation3DLayout**。需要父容器感知旋转 → rotation3DLayout。纯视觉动画不影响布局 → rotation3DEffect。大多数空间排列场景用 rotation3DLayout。

3. **Depth alignment 的默认值是 .back**。如果你的说明卡片被模型遮挡了，检查是否需要改为 .front。大多数"标签在内容前面"的场景用 .front。

4. **2D 布局技巧直接复用**。Spacer、frame、VStack/HStack 的行为在 3D 中是一致的——carousel 需要贴底？用 VStack + Spacer，和 iOS 一样。

5. **SpatialContainer vs spatialOverlay 的选择**。多个视图共享 3D 空间 → SpatialContainer。单个视图叠加一个额外视图 → spatialOverlay。spatialOverlay 的额外视图会根据目标视图的尺寸自动缩放。

---

## 相关 Session

- [Better together: SwiftUI and RealityKit](/articles?year=2025&topic=all&search=274) — discover how to seamlessly blend swiftui and realitykit in visionos 26. we
- [Build a SwiftUI app with the new design](/articles?year=2025&topic=all&search=323) — explore the ways liquid glass transforms the look and feel of your app. discover
- [Build a UIKit app with the new design](/articles?year=2025&topic=all&search=284) — update your uikit app to take full advantage of the new design system. we
- [Explore prompt design & safety for on-device foundation models](/articles?year=2025&topic=all&search=248) — design generative ai experiences that leverage the strengths of the foundation m
