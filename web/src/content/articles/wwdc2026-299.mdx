---
title: "Create robust evaluations for agentic apps"
description: "learn how to leverage advanced features of the evaluations framework to build robust evaluations for your app. explore evaluating flows with tool calling and dynamic conditions, and how to define what correct behavior means for your use case."
date: 2026-06-10
tags: ["Evaluations", "Agentic Apps", "Tool Calling", "Synthetic Data", "Foundation Models"]
thumbnail: "/images/sessions/2026/299.jpg"
videoUrl: "https://developer.apple.com/videos/play/wwdc2026/299/"
sessionId: "wwdc2026-299"
year: "2026"
relatedSessions:
  - title: "Meet the Evaluations framework"
    code: "298"
    description: "learn how to evaluate model-driven experiences using the evaluations framework"
  - title: "Build agentic app experiences with the Foundation Models framework"
    code: "242"
    description: "learn how to take your intelligence features further with foundation models framework primitives"
  - title: "Improve your prompts by hill climbing with Evaluations"
    code: "246"
    description: "explore how to use the evaluations framework to improve your prompts and intelligent features"
  - title: "Bring an LLM provider to the Foundation Models framework"
    code: "339"
    description: "extend the foundation models framework by implementing a languagemodelexecutor for your own llm"
---

## Highlight

> 做 AI 功能最怕什么？手写 13 个测试用例全过，一上真实用户数据直接翻车。尤其是 Agent 应用，模型在后台调了四五个工具，最后拼凑出一个看似正确的答案，其实中间逻辑全错。Evaluations 框架的 `SampleGenerator` 让你批量合成几百条测试数据，而 `TrajectoryExpectation` 则去查会话记录里的"监控录像"，核实模型是不是按正确顺序、传了正确参数调用了正确工具。

---

## 核心内容

### 1. 这场演讲解决什么问题

上一场的 Evaluations 框架入门解决了"怎么测 AI 输出"。但 Agent 应用带来了一个更复杂的问题：**模型不再只是生成文本，它还在后台调用工具（Tool Calling）**。你可能看到模型最终给出了正确答案，但它是通过什么路径到达的？有没有绕了远路？有没有调用不该调用的工具？

这场 Session 提供了两把斧头：

1. **合成数据生成（Synthetic Data）**：用少量种子数据批量生成几百条测试样本。
2. **工具调用轨迹评估（Tool Call Evaluation）**：不仅看最终结果，还检查中间过程是否正确。

### 2. 用 SampleGenerator 扩充数据集

13 个手动样本只能给你一个狭窄的窗口。`SampleGenerator` 用模型帮你"伪造"更多数据：

```swift
let prompt = Prompt("""
    Generate diverse range of book reviews and corresponding tags.
    Cover a wide range of genres, time periods, cultures, and
    reader personas. Do not repeat books already in the dataset.
    """)

let dataset = Book.sampleBooks.map { book in
    ModelSample(prompt: book.review, expected: BookTags(tags: book.tags))
}

let targetCount = 100
var expandedDataset = dataset

for try await sample in dataset.makeSamples(prompt, targetCount: targetCount) {
    expandedDataset.append(sample)
    print("Generated \(expandedDataset.count) samples so far.")
}
```

自定义 `SampleGenerator` 配置：

```swift
let generator = SampleGenerator<ModelSample<BookTags>>(
    prompt,
    samples: dataset,
    targetCount: targetCount,
    sessionProvider: {
        LanguageModelSession(
            model: PrivateCloudComputeLanguageModel(),
            instructions: """
                You are a synthetic data generator for a book-tracking app's evaluation suite.
                Your job is to produce realistic, diverse book entries that will stress-test
                a tagging system.

                Rules:
                - Review must be at least 100 characters long.
                - Review should cover a mix of genre, mood/tone, and themes.
                - Reviews should vary in length.
                - Create between 3 and 8 tags.
                - Tags must be lowercase.
                """
        )
    }
)
```

关键点：

- `targetCount` 是最终数据集的总大小，包含种子数据。
- `sessionProvider` 让你自定义生成时使用的模型和指令。
- 生成质量取决于提示词的质量，要详细描述输出格式和约束。

### 3. 验证生成的样本

生成的数据可能有垃圾，需要验证：

```swift
let generator = SampleGenerator<ModelSample<BookTags>>(
    prompt,
    samples: dataset,
    targetCount: targetCount,
    validator: { sample in
        guard let book = sample.expected else { return false }

        // 评论至少 100 个字符
        guard sample.promptDescription.count >= 100 else { return false }

        // 标签数量在 3-8 之间
        guard (3...8).contains(book.tags.count) else { return false }

        // 所有标签必须小写
        guard book.tags.allSatisfy({ $0 == $0.lowercased() }) else { return false }

        return true
    }
)
```

迭代后检查有效和无效结果：

```swift
for try await sample in generator.run() {
    expandedDataset.append(sample)
}

let allSamples = await generator.samples
let invalidSamples = await generator.invalidSamples

print("Generated \(allSamples.count) new samples. Total: \(expandedDataset.count)")
```

关键点：

- `validator` 是一个闭包，返回 `false` 的样本会被标记为 invalid。
- 迭代结束后可以查看 `invalidSamples`，分析生成失败的模式。
- 根据 invalid 的模式调整 validator 规则或生成提示词。

### 4. 定义工具的 Generable 参数

Agent 应用的核心是工具调用。用 `@Generable` 定义工具参数，让模型知道每个参数的用途：

```swift
@Generable
struct SearchBooksArguments {
    @Guide(description: "A freeform search term to match against titles, reviews, or tags")
    var query: String?

    @Guide(description: "Filter results to books with this specific tag")
    var tag: String?

    @Guide(description: "Filter results by mood")
    var mood: String?

    @Guide(description: "Filter results by genre")
    var genre: String?

    @Guide(description: "Maximum number of results to return. Defaults to 5.")
    var limit: Int?
}
```

关键点：

- 参数设为 Optional，让模型在信息不足时可以选择不传。
- `@Guide` 描述越具体，模型调用越准确。
- 如果强制所有参数必填，模型可能为了凑参数而产生幻觉。

### 5. TrajectoryExpectation：验证工具调用轨迹

这是 Session 的核心新能力。不仅检查结果，还检查过程：

**基本用法——验证调用了正确的工具：**

```swift
TrajectoryExpectation(
    unordered: [
        ToolExpectation(
            "searchBooks",
            arguments: [
                .exact(argumentName: "tag", value: .string("gothic"))
            ]
        )
    ]
)
```

**模糊匹配——用自然语言验证参数意图：**

```swift
TrajectoryExpectation(
    "searchBooks",
    arguments: [
        .naturalLanguage(
            argumentName: "mood",
            criteria: "Should relate to uplifting, hopeful, or positive feelings"
        )
    ]
)
```

可用的参数匹配器：

- `.exact`：精确值匹配
- `.naturalLanguage`：语义匹配
- `.contains` / `.hasPrefix` / `.hasSuffix`：部分字符串匹配
- `.oneOf`：值在指定集合中
- `.pattern`：正则匹配
- `.range`：数值范围
- `.keyOnly`：只检查参数存在，不关心值

**有序调用——验证工具调用顺序：**

```swift
TrajectoryExpectation(
    ordered: [
        ToolExpectation(
            "searchBooks",
            arguments: [
                .exact(argumentName: "tag", value: .string("gothic"))
            ]
        ),
        ToolExpectation(
            "getBookDetails",
            arguments: [
                .keyOnly(argumentName: "bookId")
            ]
        )
    ]
)
```

**禁止特定工具调用：**

```swift
TrajectoryExpectation(
    unordered: [
        ToolExpectation(
            "searchBooks",
            arguments: [
                .naturalLanguage(
                    argumentName: "genre",
                    criteria: "Should refer to science fiction")
            ]
        )
    ],
    disallowed: [
        ToolExpectation("findSimilarBooks")
    ]
)
```

### 6. 构建完整的 Tool Call Evaluation

把 TrajectoryExpectation 和 Evaluation 框架结合起来：

```swift
let samples = SampleArrayLoader(samples: [
    ModelSample(
        prompt: "Find all the books tagged with 'gothic'.",
        instructions: "Help the user explore their book collection.",
        expectations: TrajectoryExpectation(
            unordered: [
                ToolExpectation(
                    "searchBooks",
                    arguments: [
                        .exact(argumentName: "tag", value: .string("gothic"))
                    ]
                )
            ]
        )
    )
])

struct BookLibraryToolCallEval: Evaluation {
    var dataset = samples

    let pass = Metric("All Passed")
    let percent = Metric("Percentage Passed")

    var evaluators: Evaluators {
        ToolCallEvaluator(allPass: pass, percentagePass: percent)
    }
}
```

### 7. 合成 Tool Evaluation 的样本

工具调用的评估样本也可以用模型生成：

```swift
let prompt = Prompt("""
    Generate diverse user queries for a personal book library assistant.
    Each sample needs a prompt (what the user says), and a trajectory
    expectation describing which tools should be called and in what order.
    """)

let instructions = """
    AVAILABLE TOOLS:
    - searchBooks(query?, tag?, mood?, genre?, limit?): search the library
    - getBookDetails(bookId): full details for one book
    - findSimilarBooks(bookId, maxResults?): find books sharing tags
    ORDER REQUIREMENTS:
    - searchBooks must comes before getBookDetails or findSimilarBooks
    - Use TrajectoryExpectation(ordered:) when sequence matters, else (unordered:)
    USE THESE ARGUMENT MATCHERS:
    - .exact for precise values, .naturalLanguage for fuzzy matching
    - .keyOnly when any value is acceptable, .range for numeric constraints
    - .contains/.hasPrefix/.hasSuffix for partial string matching
    """
```

验证生成的工具评估样本：

```swift
validator: { sample in
    // 必须有 expectations 定义
    guard sample.output.expectations != nil else { return false }
    let expectations = sample.output.expectations!

    // 必须引用至少一个工具
    let totalExpectations = expectations.ordered.count + expectations.unordered.count
    guard totalExpectations > 0 else { return false }

    // 所有工具名必须在有效集合中
    let validTools: Set<String> = ["searchBooks", "getBookDetails", "findSimilarBooks"]
    let allExpectations = expectations.ordered + expectations.unordered + expectations.disallowed
    for expectation in allExpectations {
        guard validTools.contains(expectation.name) else { return false }
    }

    return true
}
```

---

## 最佳实践

- **老项目迁移**：把你现有的 XCTest 里手写的几个 AI 测试用例抽离出来，作为 `SampleGenerator` 的 `samples` 种子。先跑一次生成，人工 review 一下 `invalidSamples`，调整 `validator` 规则，把数据集扩充到 100-200 条再跑正式评估。

- **新项目架构**：把"结果评估"和"轨迹评估"拆成两个独立的 Test Suite。结果评估跑得快，可以在每次改 Prompt 时跑；轨迹评估涉及真实工具调用和多次推理，耗时较长，建议放在 Nightly Build 或合入主干前的 CI 里跑。

- **避坑指南**：
  - 在写工具的 `@Generable` 参数时，尽量把参数设为 Optional。强制必填会导致模型在模糊指令时产生幻觉。
  - `TrajectoryExpectation` 的 `ordered` 和 `unordered` 选择要准确。如果顺序不重要却用了 `ordered`，测试会不必要地失败。
  - `disallowed` 工具列表要完整。如果你只禁止了 `findSimilarBooks` 但模型调了另一个不该调的工具，测试不会报错。
  - 合成数据的 `validator` 规则要迭代优化。第一批生成的数据通常有 20-30% 无效，这是正常的。

---

## 相关 Session

- [Meet the Evaluations framework](/articles/wwdc2026-298) — Evaluations 框架的基础概念，包括 Metric、Evaluator 和 Model Judge
- [Build agentic app experiences with the Foundation Models framework](/articles/wwdc2026-242) — 用 Foundation Models 构建 Agent 工作流和动态上下文
- [Improve your prompts by hill climbing with Evaluations](/articles/wwdc2026-246) — 用 Evaluations 框架迭代优化 Prompt 的"爬山法"
- [Bring an LLM provider to the Foundation Models framework](/articles/wwdc2026-339) — 把第三方 LLM 接入 Foundation Models 框架
